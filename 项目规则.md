---
doc_id: PROJECT-RULES
version: 1.4.0
updated: 2026-09-22
status: ACTIVE
depends_on: MASTER-PLAN@1.6.0
---

# 晨报机 · 项目规则

> 本文件回答一个问题：**这个项目里，什么事该怎么做、什么事不能做。**
> 每条规则都有编号、理由和违反后果。**规则没有后果，就只是愿望。**
>
> 修改方式见 §G。规则可以随时调整，但调整本身也要走流程。

---

## A. 协作规则（人机之间）

### R-A01 · 需求先登记，再执行

用户提出的任何新需求，**先判断在不在 `项目计划工程书 §3.1` 范围内**，再决定怎么处理。

- 在范围内 → 进任务明细
- 不在范围内 → 进 §3.3 待定区，标注决策截止点，**不当场承诺**
- 与铁律冲突 → 触发 P0 评审

**为什么**：大多数项目不是死于"改需求"，而是死于"改需求但没登记"。半年后没人记得当初为什么做这个功能。
**违反后果**：范围无声膨胀 → 产品变成"什么都做了一半"。

### R-A02 · 每次开工前先读计划书

不是读全文，是读 **§5 当前进度看板 + §5.4 阻塞项**。

**为什么**：避免"埋头做了三天，抬头发现方向已经在两天前改了"。
**违反后果**：返工。返工是最大的时间黑洞。

### R-A03 · 大改动前先给方案，不先给代码

涉及以下任一项时，**先出方案让人确认**：技术选型、数据模型、界面结构、引入新依赖、改变目录结构。

**为什么**：这些是"改起来很贵"的东西。写之前的五分钟，值写之后的五小时。
**违反后果**：方案被否 → 代码全废。

### R-A04 · 不确定就说不确定

对"能不能实现""性能行不行""这个 API 稳不稳"这类问题，**不允许给出听起来确定的回答**。必须明确区分「已验证」和「推测」。

**为什么**：把推测当结论，会在下游被当成既定事实继续往上盖楼。
**违反后果**：地基是假的，上面盖得越漂亮越危险。

### R-A05 · 每完成一个阶段必须走收尾流程

按 `项目计划工程书 §9` 的清单逐项打勾，跑同步校验，更新变更记录。

**为什么**：阶段边界是唯一能"刹车检查"的时刻。错过了就一路滑下去。
**违反后果**：文档与代码脱节，同步机制失效。

---

## B. 开发规则（代码）

### R-B01 · 渲染进程不做重活

抓取、AI 调用、解析、去重，**全部在主进程**。渲染进程只负责画。

**为什么**：卡片每秒可能被扫视，主线程不能有任何阻塞。（铁律 L4）
**违反后果**：卡片卡顿 → 直接被关掉。

### R-B02 · 只动 `transform` 和 `opacity`

动画不允许动 `width`/`height`/`top`/`left`/`margin`/`padding`/`box-shadow`。

**唯一例外**：展开/收起的高度过渡（低频操作）。例外必须配套：加 `will-change` 且动画结束立刻移除。

**为什么**：前者走 GPU 合成，后者触发重排/重绘。
**违反后果**：掉帧。常驻窗口掉帧是致命的。

### R-B03 · 不用 `transition: all`

必须写明具体属性。

**为什么**：`all` 会动画化你没想到的属性，而且触发意外的布局计算。
**违反后果**：诡异的视觉 bug，且极难定位。

### R-B04 · 每个异步操作都要有失败路径

抓取、AI 调用、文件读写、IPC——**每一个都要有失败分支**，不允许"假设它一定成功"。

**为什么**：这是常驻软件。它要在无人看管的情况下连续跑几个月。
**违反后果**：静默失败 → 用户以为没问题，其实已经三天没更新了。

### R-B05 · 引依赖前先问三个问题

1. 这个包有多大？
2. 能不能自己写 30 行解决？
3. 它还在维护吗？

**为什么**：常驻工具的启动速度和内存占用都是生死线。每一个依赖都是成本。
**违反后果**：安装包膨胀、启动变慢、内存超标（违反 §1.2 辅助指标）。

### R-B06 · 中文注释写"为什么"，不写"是什么"

代码本身说明"是什么"，注释只说明"为什么这么写"。

**为什么**：半年后你会忘记的是动机，不是语法。
**违反后果**：重构时不敢动代码，因为不知道当初为什么那样写。

---

### R-B07 · ESM 主进程禁止顶层 `await app.whenReady()`

主进程入口（以及任何会被入口静态导入的模块）**一律写成 `app.whenReady().then(...)`**，不得在模块顶层 `await` 它。

**为什么**：Electron 要等入口模块**求值完毕**才发出 `ready` 事件，而顶层 `await app.whenReady()` 正在等这个事件 —— 死锁。
**症状极其难查**：不报错、不崩溃、进程也活着，但**一个窗口都不会出现**。已用带看门狗的对照实验坐实（`m0-probe/experiments/diag-tla-deadlock.mjs`）。
**违反后果**：应用静默不启动，且第一反应会去查窗口配置而不是查 `await`，白白浪费半天。
**关联**：审查报告 B10 ｜ 证据 `m0-probe/report/diag-tla-deadlock.txt`

### R-B08 · 窗口尺寸与坐标的硬规则

两条，**都必须遵守**：

1. **构造参数里的 `width` / `height` 一律不信。** 创建 `BrowserWindow` 后必须再 `setBounds({width, height})` 校正一次。
2. **窗口坐标必须对齐到物理像素网格。** 步长 = 使 `step × scaleFactor` 为整数的最小正整数（`100%→1 · 125%→4 · 150%→2 · 175%→4`）。

```js
// 正确写法：构造 → 校正 → 坐标对齐（实现见 src/main/window.js）
const g = alignToPhysicalGrid(x, y, display.scaleFactor);
win.setBounds({ x: g.x, y: g.y, width: W, height: H });
```

**为什么**：M0 实测（`m0-probe/report/winsize3-matrix.txt`，规律 100% 一致）：
请求 `360×44` 的窗口，构造出来是 `362×46` 甚至 `364×46`；坐标若落在非整数物理像素上（150% 下即奇数），宽或高还会再多 `1px`。
**违反后果**：卡片尺寸悄悄错 1~4px。视觉上不易察觉，但"尺寸精确"这条验收会莫名其妙地挂，排查成本极高。
**关联**：审查报告 B12 ｜ ADR-009

### R-B09 · Electron 必须经唯一启动入口启动

**禁止**直接 `electron .`。所有启动（开发、自检、实验）一律走 `tools/launch-electron.mjs`。

**为什么**：环境里可能存在 `ELECTRON_RUN_AS_NODE=1`（本机就有，且不少 Electron 宿主应用会把它泄漏给子进程）。
只要它存在，`electron.exe` 会**静默退化成「普通 Node」**：不开窗口、不注入 Electron API，然后报一句完全误导的
`does not provide an export named 'BrowserWindow'`。
**这个变量在进程启动时就被读取，进程内无法自救** —— 只能在启动器里于 `spawn` 之前删掉。
**违反后果**：你以为在测 Electron，其实什么都没测，还可能拿到一份"看起来跑通了"的假数据。
**关联**：审查报告 B11 ｜ 启动器同时负责"开机心跳"守护，让这类静默失败变成可判定报警

### R-B10 · 主进程失败语义必须明确：同步落盘 + 明确退出策略

`uncaughtException` 与 `unhandledRejection` **必须由项目自己给出策略**，不许依赖运行时默认行为：

| 种类 | 必须做什么 | 为什么 |
|---|---|---|
| `uncaughtException` | **同步落盘完整上下文 → `process.exit(1)`** | 同步异常之后进程状态未知。继续跑的代价是「行为异常却没有任何线索，且后续错误会级联污染日志」 |
| `unhandledRejection` | **同步落盘 → 不退出，但必须可见** | promise 拒绝不必然破坏同步状态；一刀切退出会把"网络抖了一下"升级成"应用崩了"。**但不许吞掉** |

**「同步落盘」是正确性要求，不是风格问题**：落盘之后马上就要退出，异步写会被直接丢掉 ——
于是崩溃记录**恰好在最需要它的时候不存在**。

**为什么不能靠"删掉那个监听器"**：实测证明 **Electron 自己就注册了一个 `uncaughtException` 监听器**
（进程刚起来、我们还没注册时 `listenerCount` 已经是 1）。删掉自己那段**恢复不了**「崩溃即退出」。
**正确的问题是"要不要主动处理"，不是"要不要删"。**

**违反后果**：主进程"带伤继续跑" —— 用户看到卡片行为异常，而你手里一条线索都没有。
**实现**：`m0-probe/src/main/crashlog.js` ｜ **考裁判**：`npm run test:crash`
**关联**：审查报告 B13 ｜ 取证实验 `m0-probe/experiments/diag-uncaught-exception.mjs`

### R-B11 · 不需要 Electron 的逻辑，不许被 electron 的 import 绑架

**凡是"不需要 Electron 就能完成"的逻辑，必须拆成零依赖模块**（如 `focus-verdict.js`、`crashlog.js`），
再由需要 Electron 的文件去 import 它。

**为什么**：`import { app } from 'electron'` 会让**整块逻辑绑死在「必须能启动一个 GUI 进程」的环境上**。
后果不是"多一层依赖"，而是**这块逻辑在所有无头环境（CI、沙箱、远程会话）里永远无法被验证** ——
只剩"人手跑一次应用然后祈祷"这一条路。拆开之后它可以用纯 Node 秒级验证，还能跑变异测试。

**判定问句**：这段代码里**出现过 `electron` 这个名字吗**？没有 → 它就不该待在 import 了 electron 的文件里。

**违反后果**：验收项在无头环境里"永远测不了"，而它看起来只是"环境限制"，一点都不像缺陷。
**实现先例**：`src/main/focus-verdict.js`（焦点判定）、`src/main/crashlog.js`（失败语义）
**关联**：审查报告 B13 ｜ `docs/05-M0验收记录.md` §6.4（保证登记信息准确的硬约束）

---

## C. 设计规则

### R-C01 · 玻璃只用于功能层，绝不下沉到内容层

**为什么**：Apple HIG 最明确的一条规则。滥用玻璃会让层级崩塌、可读性完蛋。
**违反后果**：整套视觉的骨架塌掉，且无法局部修复。

### R-C02 · 正文对比度必须 ≥ 4.5:1

在任何透明度档位、任何壁纸上，都要达标。**这是上线前的强制检查项。**

**为什么**：卡片要在用户完全不可控的背景上保证可读。（铁律 L3）
**违反后果**：iOS 26 液态玻璃的下场——被骂、然后回滚。

### R-C03 · 布局结构绝对稳定

可变的是内容，不可变的是结构。总览句永远在最上面、刷新永远在右下角。

**为什么**：常驻工具的可用性建立在位置稳定性和肌肉记忆上。（铁律 L2）
**违反后果**：用户每天要重新找东西 → 关掉。

### R-C04 · 全项目动效上限 8 条

新增任何一条动效，必须先回答："**它是不是替代了某个信息？**"答不上来就不加。

**为什么**：动效预算和注意力预算一样有限。（铁律 L2）
**违反后果**：界面变得"吵"，用户感到疲劳。

### R-C05 · 每个动效都必须有 `prefers-reduced-motion` 降级

**没有例外。**

**为什么**：前庭功能敏感的用户会因弹性动效产生真实眩晕感。这是无障碍底线，不是优化项。
**违反后果**：一部分用户物理上无法使用这个产品。

### R-C06 · 组件必须有完整状态定义

每个组件都要定义：默认 / hover / 按下 / 焦点 / 加载 / 禁用 / 空 / 错误。哪怕某些状态"看起来用不到"。

**为什么**：缺失的状态就是将来会出现的 bug。
**违反后果**：上线后各种"没写过的状态"以 bug 形式出现。

### R-C07 · 收起态优先

开发和评审顺序永远是：**先收起态，再展开态**。

**为什么**：收起态才是用户每天真正看到的形态，而且空间越小越考验信息设计。
**违反后果**：做出一个漂亮的展开态，和一个没人想看的收起态。

### R-C08 · 形态差异只能活在令牌层

组件的 CSS/JS 里**禁止出现** `backdrop-filter`、半透明 `rgba()` 值、硬编码阴影。一律通过令牌变量表达。
切换「玻璃 / 不透明」形态 = 在 `<html>` 上切一个 `data-surface` 属性，**不改任何组件代码**。

**为什么**：这是 ADR-008 的技术前提。如果形态差异渗进组件，降级就意味着重写全部样式；留在令牌层，降级就只是改一个变量值。
**违反后果**：`backdrop-filter` 一旦不可用（R8）或混合缩放不可解（R9），返工量从"改 20 行变量"变成"重写 2000 行样式"。

### R-C09 · 降级形态必须是一等公民

新增任何"形态相关"的样式时，**两个形态各写一份**。降级形态必须独立通过对比度检查（≥ 4.5:1），且 **8 条动效一条都不减**。

**为什么**：不透明形态不是"丑版兜底"，是另一个正式形态。用户遭遇降级不该被罚一次体验降级。
**违反后果**：降级形态下界面崩坏，而且**因为平时只看一个形态，很难被发现**（见架构文档技术债 TD-01）。

---

## D. 文档规则

### R-D01 · 每份文档头部必须有元数据块

格式固定：

```
---
doc_id: <唯一标识>
version: <语义化版本>
updated: <YYYY-MM-DD>
status: <ACTIVE | DRAFT | DEPRECATED>
depends_on: <上游doc_id@版本，多个用逗号分隔，无则填 none>
---
```

**为什么**：同步校验工具靠这个工作。少一个字段就校验不了。
**违反后果**：`sync-check` 报错，同步机制失效。

### R-D02 · 每份文档必须有 `## 变更记录` 章节

**只增不删。**

**为什么**：这是判断项目有没有跑偏的唯一依据。
**违反后果**：无法追溯决策原因，重复踩同一个坑。

### R-D03 · 上下游同步：核对后即使不改也要留痕（**且留痕必须含确切版本号**）

上游升版本后，下游必须重新核对，并在**变更记录**里写一行留痕。

**留痕的格式是硬性的**：必须出现**确切的版本号字面量** `上游doc_id@版本`。例如：

```
| 2026-09-19 | — | **记账**：核对 MASTER-PLAN@1.2.0、ARCHITECTURE@1.2.1，结论：**无需改动**（依 R-D05 不升版） |
```

**为什么必须含版本号**：只写"已核对"三个字，机器无法判断你核对的是**哪一版**。
写上版本号，`sync-check` 才能在每个上游升版时**准确点出谁还没核对**。
→ **只把 `depends_on` 里的数字改一下、却不写结论，这条检查会立刻抓住。**
（这条规则的存在就是为了堵这个偷懒 —— 否则"手改版本号"能让校验变绿。）

**留痕位置**（`sync-check` 按此查找，写错地方等于没写）：

| 谁 | 留痕写在哪 |
|---|---|
| **文档** | 它自己的 `## 变更记录` 章节 |
| **代理定义** | 它自己的 `## Lessons` 章节，或 `agents/README.md` 的 `## 7. 代理系统变更记录`（后者是既有约定，两者认其一） |

⚠️ **frontmatter 不算留痕。** `depends_on: X@1.0.0` 这一行**自身就含** `X@1.0.0`，
把它算数等于让检查自己满足自己。

**为什么**：没有记录 = 没人知道到底核对过没有。**写下核对结论的过程，就是核对本身。**
**违反后果**：`sync-check` 直接 FAIL，报 `[缺留痕]`。同步矩阵形同虚设。

### R-D04 · 文档优先于代码

文档和代码冲突时，**以文档为准**，除非文档本身已被证明是错的（那就先改文档）。

**为什么**：文档是意图，代码是结果。意图错了要改意图，结果错了要改结果，不能混。
**违反后果**：代码悄悄变成了"实际标准"，而没人知道它为什么长这样。

### R-D05 · 记账 vs 实质：改 `depends_on` 与补留痕**不升版本号**

**版本号只表示一件事：这份文档自己的实质内容变了。**

| 属于「**记账**」→ **不改版本号** | 属于「**实质**」→ **必须升版本号** |
|---|---|
| 更新 `depends_on` 跟上上游新版本（R-D03） | 规则的要求变了、新增/删除了规则 |
| 补写 R-D03 要求的核对留痕 | 结论、契约、职责边界变了 |
| 回填同步矩阵 / 进度数字 / 正文里引用的版本号 | 代码行为或设计口径变了 |

**为什么这条规则存在 —— 它让级联只跑一轮**：

```
上游升版 → 下游核对（改 depends_on + 写留痕，**不升版**）
        → 下游的版本号没变 → 它的下游**不会**过期 → 终止 ✅
```

**旧机制为什么不成立**：下游"仅依赖跟进"也要升 patch 号，于是下游的**下游**跟着过期，
级联**递归**下去。实测一次"只改一句措辞"带出了 **9 个文件**，并且继续向第二轮扩散。

**边界（别把这条规则当免死金牌）**：

- 判据是「**有没有实质内容变化**」，**不是**「改动大不大」或「有没有人看」。
- 一次编辑里**既有记账又有实质**改动 → **按实质处理，升版本号**。
- **不许为了少改几个文件，把实质改动伪装成记账。**
  这是本条规则唯一能被滥用的地方，而它造成的后果是"**下游以为上游没变，于是没去核对**" ——
  **正是整套机制要防的那件事。**

**为什么**：把记账从版本号里剥离出去，版本号才重新变得可读，级联才在有限步内终止。
**违反后果**：版本号失去信息量（谁都读不出"这份文档到底变没变"），级联失控。

---

## E. 数据与安全规则

### R-E01 · 用户的原始数据只读不写

**在 v0.5 之前，程序绝对不移动、不重命名、不删除用户的任何文件。**

采集只读 RSS 和 API，产出只写自己的数据库。

**为什么**：数据安全是不可逆的。一次误操作毁掉的是全部信任。
**违反后果**：信任归零，且无法挽回。

### R-E02 · API Key 用 `safeStorage` 加密存储

**不允许明文写在配置文件、代码、SQLite 或日志里。**

采用 Electron `safeStorage.encryptString()`，密文落 `userData` 下的**独立文件**（不进 `settings` 表——表会被导出/备份）。
若 `safeStorage.isEncryptionAvailable()` 为 false，**必须明示用户**并让其选择"仅本次会话内存持有"或"明文落盘（不推荐）"，**不允许静默降级为明文**。

**为什么**：配置文件会被同步、被分享、被截图；数据库会被备份。
**违反后果**：用户的 Key 泄露，产生真实费用。

### R-E05 · API Key 不得离开主进程

**三条硬约束：**

1. **没有"读回明文"的通道。** `apikey:status` 只返回布尔与掩码尾号（如 `{ configured: true, maskedTail: '••••1234' }`）——渲染进程从设计上就拿不到 Key。
2. **网络请求只能在主进程发起**，Key 仅作为请求头在内存中短暂存在。渲染进程发不出任何携带 Key 的请求。
3. **Key 不进日志、不进错误上报、不进崩溃转储。**

**为什么**：ADR-007 选了"用户自带 Key"这条路，**密钥安全就成为本项目新增的一等关注点**——Key 泄露的后果是用户真实付费。这是自带 Key 方案的必要配套。
**违反后果**：一次泄露就毁掉这个工具的可信度，且用户损失是真金白银。

### R-E03 · 不抓取需要登录态的内容

只抓公开的、明确允许抓取的内容。

**为什么**：合规风险 + 反爬成本。（铁律 L5）
**违反后果**：法律风险 + 无底洞式的维护成本。

### R-E04 · 采集失败不能拖垮整体

单源失败必须被隔离，不能阻塞其他源。

**为什么**：RSS 源的稳定性不在我们控制范围内，挂掉是常态。
**违反后果**：一个源挂了，整份简报都出不来。

---

## F. 子代理规则

### R-F01 · 每个代理必须声明输入与输出契约

不允许"看情况决定输出什么"。

**为什么**：代理要被组合使用，契约不明确就无法编排。
**违反后果**：代理之间无法协作，退化成一次性脚本。

### R-F02 · 每个代理必须有 lessons 段，且每次任务后更新

**这是"自动学习"的落地方式。** 不写，代理就永远是第一天的水平。

**为什么**：学习不是靠模型自己进化，是靠把经验写下来。
**违反后果**：同一个坑反复踩。

### R-F03 · 代理升级必须走版本号 + 留痕

修改代理定义 = 升版本 = 在 `registry.json` 和 `agents/README.md` 的变更记录里留一行。

**为什么**：代理是"活的"，但它的进化必须可追溯。
**违反后果**：代理悄悄变了行为，没人知道为什么。

### R-F04 · 代理不得自行扩大职责范围

代理发现自己"顺便还能干点别的"时，**提出来，不自行实施**。

**为什么**：职责蔓延是系统腐化的开始。
**违反后果**：代理边界模糊，难以调试和替换。

### R-F05 · Critic 代理的意见必须被显式处理

审查代理提出的问题，**要么修，要么记录"已知晓，暂不修"及理由**。不允许静默忽略。

**为什么**：不处理就等于没有审查。
**违反后果**：审查流程变成走过场。

---

## G. 规则本身的修改流程

规则是可以改的——**前提是改的方式和遵守规则一样认真。**

### G.1 修改步骤

1. **提出**：写明要改哪条、为什么改、期望效果
2. **判断级别**：

   | 级别 | 范围 | 需要什么 |
   |---|---|---|
   | **微调** | 只改措辞、补充示例、细化说明 | 直接改，升 patch 版本（1.0.0 → 1.0.1） |
   | **实质** | 改变规则要求、新增/删除规则 | 必须说明影响面，升 minor（1.0.1 → 1.1.0） |
   | **结构性** | 删掉整节、改变规则的组织方式 | 检查与铁律是否冲突，升 major（1.1.0 → 2.0.0） |

3. **改完必须做的三件事**：
   - 更新本文件的 `version` 与 `updated`
   - 在下方 §G.2 追加一行变更记录
   - 运行 `node tools/sync-check.mjs` 校验下游是否需同步

### G.2 规则变更记录

| 日期 | 版本 | 级别 | 改了哪条 | 为什么 | 影响面 |
|---|---|---|---|---|---|
| 2026-09-25 | **—**（记账） | **记账** | **依 R-D05：不改版本号。** 核对 `MASTER-PLAN@1.5.0` —— 该上游本轮改的是**「M1 交付到哪一步」这件事实**（M1 由「未开始」改判为「部分交付」＋ 新增交付清单）。**结论：本项目规则无需改动** —— 不新增、不删除、不改写任何 `R-*` 条款，40 条规则的判据与边界一字未动 | M1 交付状态归档，不触及任何规则 | 无 |
| 2026-09-18 | 1.0.0 | — | 全部规则首次建立 | 用户要求建立可随时修改的项目规则 | 全部 |
| 2026-09-18 | 1.1.0 | **实质** | ① **新增 R-C08**（形态差异只能活在令牌层）；② **新增 R-C09**（降级形态必须是一等公民）；③ **改写 R-E02**（由"系统凭据管理器"具体化为 `safeStorage` + 不可用时的显式处理）；④ **新增 R-E05**（API Key 不得离开主进程，三条硬约束） | 落地 MASTER-PLAN v1.1.0 的 ADR-007（用户自带 Key）与 ADR-008（降级为一等公民）——两条 ADR 各自推出一条必须写进规则的硬约束 | 设计组（C）、数据与安全组（E）；下游 `DESIGN-SPEC`、`ARCHITECTURE`、`design-critic`、`brief-writer` 需核对 |
| 2026-09-18 | 1.2.0 | **实质** | 新增三条开发规则：① **R-B07**（ESM 主进程禁止顶层 `await app.whenReady()`）；② **R-B08**（构造参数尺寸不可信 + 坐标必须对齐物理像素网格）；③ **R-B09**（Electron 必须经唯一启动入口启动）。速查表同步（34 → 37 条） | M0 实施中抓到 5 个"静默失败"级问题（审查报告 B8–B12），其中 3 个的处置**不能只写在文档里**：只在文档里提醒，下次一定会重犯；写进规则并由启动器/工具链强制执行，才不依赖人记得。**规则与代码互相印证**：R-B07 由启动器的心跳守护兜底，R-B09 由 `package.json` 的脚本唯一化兜底 | 开发组（B）；下游 `ARCHITECTURE`（ADR-009）、`AUDIT`（§6）、`M0-RECORD` 需核对 |
| 2026-09-19 | 1.3.0 | **实质** | 新增两条开发规则：① **R-B10**（主进程失败语义：`uncaughtException` 同步落盘 + `exit(1)`；`unhandledRejection` 落盘但不退出且必须可见）；② **R-B11**（不需要 Electron 的逻辑不许被 electron 的 import 绑架，必须拆成零依赖模块）。速查表同步（37 → 39 条） | 处置审查报告 **B13** 时产生。R-B10 来自 B13 取证：**"删掉那段监听器"解决不了问题**（Electron 自己也注册了一个），所以必须由项目给出明确策略，不能依赖运行时默认行为。R-B11 来自这次处置的**意外收获** —— 为了在无 GUI 环境里验证 R-B10，把失败语义拆成零依赖的 `crashlog.js`，于是它能在纯 Node 下真跑一次崩溃并断言退出码；同理拆出的 `focus-verdict.js` 让验收 3 的**判定**部分也能离线考裁判。**不拆的代价不是"多一层依赖"，而是那块逻辑在所有无头环境里永远无法被验证** | 开发组（B）；下游 `ARCHITECTURE`、`AUDIT`、`M0-RECORD`、`M0-PROBE` 需核对 |
| 2026-09-19 | 1.4.0 | **实质** | ① **改写 R-D03** —— 留痕从"写一句'已核对'"升级为**硬性格式**：必须出现**确切版本号字面量** `doc_id@版本`；并明确留痕位置（文档 = 自己的「变更记录」；代理定义 = 自己的「Lessons」或 `agents/README.md` §7），且**明确 frontmatter 不算留痕**（否则 `depends_on` 那行自己就满足了自己）。② **新增 R-D05「记账 vs 实质」** —— 更新 `depends_on` 与补写核对留痕属**记账，不改版本号**；只有实质内容变化才升版。**推论：级联只跑一轮。** ③ 配套改造 `tools/sync-check.mjs`：依赖解析扩为**两道闸**（版本必须跟上 ＋ 必须留下核对痕迹）。速查表同步（39 → 40 条） | 处置 2026-09-19 登记的机制缺陷「`depends_on` 只有文档级粒度，没有接口级粒度 → 任何上游改动都波及全部下游，级联**递归**失控」。用户拍板采纳我推荐的 **C+E 合并形态**。**收益当场可见**：本行这次 `PROJECT-RULES` 1.3.0 → 1.4.0 带出**一轮**级联（5 个下游，**全部记账、0 个升版**），级联**在此终止**；按旧机制，同样这次改动会让那 5 个各升 patch，再向第二轮扩散。**根因很简单**：下游不再"为了跟上而升版"，它的下游就不会过期 | 文档组（D）。上游 `MASTER-PLAN@1.2.0` **未变**，无需核对。下游 `AUDIT`、`M0-RECORD`、`AGENT-SYSTEM`、`curator`、`design-critic` 需**核对并留痕（记账，不升版）** |
| 2026-09-20 | **—**（记账） | — | **无规则改动。** 核对上游 **`MASTER-PLAN@1.3.0`**，结论：**本文件无需改动** | SSOT 于 2026-09-20 由 1.2.0 升 **1.3.0**（实质：`§3.2` WorkerW 翻案、新增 M0 追加验收 A10、登记 5 条新需求、+B-06、+层级决策、+R12）。**这些都不新增、也不改变任何一条规则** —— 需求 1 的翻案走的是 **ADR（`docs/03` ADR-011）与里程碑流程**，不是规则层动作。**规则层本轮唯一相关的动作是"遵守"**：L5 被正确触发了 P0 评审，而不是被绕过 | 无（未改动任何规则） |
| 2026-09-22 | **—**（记账） | — | **无规则改动。** 核对上游 **`MASTER-PLAN@1.4.0`**，结论：**本文件无需改动**（仅 `depends_on` 跟进） | SSOT 于 2026-09-22 由 1.3.0 升 **1.4.0**（实质：**A10 结案 —— 桌面层挂载 FAIL ⇒ 新增 ADR-012「置底顶层窗口」**；ADR-011 结案并加删除线保留；补齐 `§3.2`／`§5.5`／`§8 R12` 三处**"宣告了却没落笔"**的正文；§6 同步矩阵跟进）。**这些都不新增、也不改变任何一条规则** —— 窗口层级走的是 ADR 与里程碑流程。⚠️ **但本轮本文件自己成了缺陷现场**：它是全项目**唯一**的 CRLF 文件，而 `sync-check` 的 frontmatter 解析在 CRLF 下会**静默丢掉最后一个键**（正是 `depends_on`）⇒ **本文件的上游依赖此前从未被机器看见过，"未跟进"永远抓不到它**。已修为 LF，并给检查器同时补上「归一化」与「CRLF 直接报错」（见 SSOT §7 同批补充） | 无（未改动任何规则） |
| 2026-09-25 | **—**（记账） | **记账** | **依 R-D05：不改版本号。** 核对 `MASTER-PLAN@1.6.0` —— 该上游本轮是**执行级**变更（把 M1 定义里的「AI 摘要」做出来）。**结论：本项目规则无需改动** —— 不新增、不删除、不改写任何 `R-*` 条款；R-E02 / R-E05 那两条（Key 用 safeStorage 加密、Key 不得离开主进程）**正是这次实现照着做的依据**，一条都没破 | M1 交付项落地，不触及规则本身 | 无 |

### G.3 规则不会失效，只会过期

被废弃的规则**加删除线保留**，并注明废弃日期和原因。

**为什么**：你需要知道"以前为什么那样做"，才能判断"现在该不该改回去"。

---

## 附 · 规则速查

| 编号 | 一句话 |
|---|---|
| R-A01 | 新需求先登记再执行 |
| R-A02 | 开工前先读进度看板 |
| R-A03 | 大改动先给方案 |
| R-A04 | 不确定就说不确定 |
| R-A05 | 阶段收尾必须走流程 |
| R-B01 | 渲染进程不做重活 |
| R-B02 | 只动 transform / opacity |
| R-B03 | 不用 transition: all |
| R-B04 | 每个异步操作都要有失败路径 |
| R-B05 | 引依赖前先问三个问题 |
| R-B06 | 注释写"为什么" |
| R-B07 | ESM 主进程禁止顶层 await whenReady |
| R-B08 | 构造尺寸不可信 + 坐标对齐物理像素网格 |
| R-B09 | Electron 必须经唯一启动入口启动 |
| R-B10 | 主进程失败语义：同步落盘 + 明确退出策略 |
| R-B11 | 不需 Electron 的逻辑不许被 electron 的 import 绑架 |
| R-C01 | 玻璃只用于功能层 |
| R-C02 | 正文对比度 ≥ 4.5:1 |
| R-C03 | 布局结构绝对稳定 |
| R-C04 | 全项目动效上限 8 条 |
| R-C05 | 每个动效都要有降级 |
| R-C06 | 组件必须有完整状态定义 |
| R-C07 | 收起态优先 |
| R-C08 | 形态差异只能活在令牌层 |
| R-C09 | 降级形态必须是一等公民 |
| R-D01 | 文档头部必须有元数据块 |
| R-D02 | 文档必须有变更记录 |
| R-D03 | 核对后即使不改也要留痕（留痕必须含确切版本号） |
| R-D04 | 文档优先于代码 |
| R-D05 | 改 depends_on 与补留痕属于记账，不升版本号 |
| R-E01 | 用户原始数据只读不写 |
| R-E02 | API Key 用 safeStorage 加密存储 |
| R-E03 | 不抓取需登录态的内容 |
| R-E04 | 采集失败不拖垮整体 |
| R-E05 | API Key 不得离开主进程 |
| R-F01 | 代理必须声明输入输出契约 |
| R-F02 | 代理必须有 lessons 段 |
| R-F03 | 代理升级必须留痕 |
| R-F04 | 代理不得自行扩权 |
| R-F05 | Critic 意见必须显式处理 |

---

*文档版本：1.4.0 ｜ 状态：ACTIVE ｜ 更新时间：2026-09-22 ｜ 上游：MASTER-PLAN@1.4.0*
