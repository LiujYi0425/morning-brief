---
doc_id: MASTER-PLAN
version: 1.6.0
updated: 2026-09-25
status: ACTIVE
authority: SSOT
depends_on: none
---

# 晨报机 · 项目计划工程书

> **这份文件是本项目的唯一权威源（Single Source of Truth）。**
> 目标、范围、里程碑的任何变更，都必须先改这里，再改其他文档。
> 其他文档一律从属于本文件，并在头部通过 `depends_on` 声明依赖版本。

---

## 0. 使用协议（怎么用这份文件）

### 0.1 三条铁律

1. **变更先改这里。** 任何目标/范围/里程碑的调整，第一步是修改本文件，第二步才是同步下游文档。顺序反了就会出现"代码和计划各说各话"。
2. **每个阶段完成必须更新三处**：§5 当前进度看板、§6 文档同步矩阵、§7 变更记录。缺一不可。
3. **不能删，只能改。** 本文件的历史版本通过 §7 变更记录保留。**永远不要删除已记录的变更行**——它是判断"项目有没有跑偏"的唯一依据。

### 0.2 变更分级

| 级别 | 含义 | 必须动作 |
|---|---|---|
| **P0** | 目标级变更（北极星指标、铁律、产品定义） | 重审全部下游文档；重建里程碑；在 §7 记 P0 |
| **P1** | 范围级变更（增删功能模块、改里程碑边界） | 更新 §3 范围 + §4 里程碑 + 受影响的下游文档 |
| **P2** | 执行级变更（技术选型微调、任务顺序、文案） | 只更新 §5 进度 + 对应下游文档 |

### 0.3 需求漂移的应对

**漂移不是错误，是常态。** 但必须被"登记"而不是被"吸收"。

- 用户提出新需求 → 先判断是否在 §3.1 范围内
- **在范围内** → 直接进 §5 任务明细
- **不在范围内** → 进 §3.3 待定区（Parking Lot），标注提出日期与理由，**不当场承诺**
- **与铁律冲突** → 必须触发 P0 评审。这是一次严肃的对话，不是一次顺手修改

> 这一条是整个机制的核心价值所在。**大多数项目不是死于"改需求"，而是死于"改需求但没登记"**——半年后没人记得当初为什么做这个功能。

---

## 1. 项目宪章

### 1.1 一句话定义

**晨报机：你的桌面情报板。** 你不用打开它、不用查收它，抬眼就能看到今天外面发生了什么。

### 1.2 北极星指标

> **连续 7 天，每天早上主动看一眼。**

选它的理由：它同时约束了"有用"（否则不会连续看）和"不打扰"（否则会关掉）。本地埋点即可验证，不需要联网统计。

**辅助指标**：

| 指标 | 目标 | 说明 |
|---|---|---|
| 压缩率 | 原始条目 → 简报条目 ≤ 5% | 300 条进，12 条出。**压缩率就是产品力** |
| 首次可用时间 | ≤ 5 分钟 | 朋友从双击安装到看到成品 |
| 单日 Token 消耗 | 记录并监控 | 不设硬阈值，但必须可见 |
| 常驻内存 | ≤ 200MB | 常驻工具的生死线 |

### 1.3 五条铁律（不可动摇）

任何需求、任何优化，如果与这五条冲突，**要么放弃，要么走 P0 评审**。

| 编号 | 铁律 | 为什么 |
|---|---|---|
| **L1** | **压缩优先**：宁可给 5 条高质量的，也不给 30 条平铺的 | 用户不缺信息，缺的是"哪些值得看"。放弃压缩，这个产品就退化成了又一个 RSS 阅读器 |
| **L2** | **不打扰优先**：位置稳定、动效克制、默认安静 | 它是用户桌面上"多出来的东西"，容忍度天然低于主动打开的工具。吵一次就被关掉 |
| **L3** | **可读性优先**：任何视觉效果都要给文字清晰度让路 | iOS 26 液态玻璃翻车的教训。Apple 自己都在 iOS 27 往回撤 |
| **L4** | **渲染进程不做重活**：抓取、AI 调用、解析全部在主进程 | 卡片每秒可能被扫视，主线程不能有任何阻塞 |
| **L5** | **边界清晰**：§3.2 列为范围外的，一件都不做 | 边界比功能更重要。"什么都做了一半"是这类项目的标准死法 |

---

## 2. 目标体系

### 2.1 长期目标（12 个月）

1. 成为**自己每天真的会用**的工具——这是最低门槛，也是最高标准
2. 做到**朋友拿到就能用**（零讲解、五分钟出成品）
3. 信息源覆盖从 RSS 扩展到 API 型源，但**永远不碰反爬型源**

### 2.2 中期目标（3 个月）

1. v0.1 跑通：自己连续三天愿意读
2. v0.5 完成：安装包 + 首屏示例简报 + 配置向导
3. ~~解决 API Key 门槛问题~~ → **已决策（2026-09-18，ADR-007）：本版不提供模型调用中转，用户自带 Key。** 泛大众化转入 §3.3 待定区，附重新评估触发条件

### 2.3 短期目标（当前里程碑）

**M0：地基验证。** 回答一个问题——**Electron 透明置顶窗口 + `backdrop-filter` 在 Windows 上到底行不行。**

### 2.4 显式非目标

以下事情**明确不做**，且不需要讨论：

- 不做移动端
- 不做多用户/账号体系
- 不做云端同步
- 不做内容社区/分享平台
- 不做资讯推荐算法

---

## 3. 范围管理

### 3.1 范围内

| 模块 | 内容 | 优先级 |
|---|---|---|
| 采集 | 标准 RSS / 博客 / 新闻站 | P0 |
| 采集 | API 型源（GitHub Trending、天气、日历） | P1 |
| 处理 | 归一化 / 去重 / 分组 | P0 |
| 处理 | AI 筛选 + 深度摘要（分层处理） | P0 |
| 展示 | 常驻桌面卡片（收起态 / 展开态） | P0 |
| 展示 | 空状态 / 错误状态 / 骨架屏 | P0 |
| 管理 | 源管理界面 | P1 |
| 管理 | 透明度三档预设 | P1 |
| 管理 | 定时调度配置 | P1 |
| 分发 | 预置源包 + 首屏示例简报 + 首次启动向导 | P1 |
| 分发 | 一键安装包 | P1 |
| 数据 | 本地 SQLite 存档与历史回看 | P2 |
| 数据 | Token 用量统计 | P2 |

### 3.2 范围外（明确不做）

| 不做的事 | 理由 |
|---|---|
| 导出分享（图片/网页） | 已确认纯私人阅读（2026-09-18 决策） |
| 公众号 / 知乎 / 微博抓取 | 反爬与合规风险，会吃光全部开发时间 |
| 手机推送（邮件/微信） | 已确认不做 |
| 桌面壁纸层原生组件（WorkerW） | 依赖系统内部结构，系统更新易损坏。<br>**2026-09-20 曾翻案（ADR-011 改为桌面层）；2026-09-22 因 A10 实测不可行而回归本行** —— ADR-012 改用「置底顶层窗口」。⚠️ **本条"理由"从未被推翻** —— 它是被接受过的风险（曾登记为 R12），现因不再采用该方案而**自然规避**。详见 `§4` M0 节末「A10 结论」与 `§8` R12 |
| AI 自适应界面（动态重排布局） | **反模式**。常驻工具靠位置稳定性 + 肌肉记忆 |
| 3D / WebGL 效果 | 常驻卡片必须零 GPU 负担 |
| 动态字体 / 持续运动元素 | 常驻元素持续运动 = 干扰源 |
| 长滚动叙事式内容流 | 卡片空间不允许，且违背"扫视"定位 |
| 多用户 / 账号体系 / 云同步 | 与"个人工具"定位冲突 |
| 弹簧回弹类动效 | 本项目没有"成功时刻"，回弹在空间逻辑上不对 |
| **提供模型调用中转服务 / 代付模型费用** | **已决策不做（ADR-007）**。会引入服务端运维责任与持续成本，且与"数据不出本机"的定位冲突。原风险 R6 由此闭环 |

### 3.3 待定区（Parking Lot）

**提出即登记，不当场承诺。** 每项需标注提出日期与决策截止点。

| 提出日期 | 需求 | 提出人 | 状态 | 决策截止 | 结论 |
|---|---|---|---|---|---|
| 2026-09-18 | **泛大众化**：让不懂技术的陌生人也能双击即用 | 用户 | 🅿️ 待定 | 2026-12-31 或 v1.0 发布后 | **已登记，未决策。** 本版明确不面向泛大众（ADR-007）。**重新评估触发条件**：① 有 ≥5 位非技术朋友主动表示想用；② 或 v1.0 稳定运行满 1 个月 |
| 2026-09-18 | 「已读 / 未读」状态（审查报告 A3） | 审查发现 | 🅿️ 待定 | M1 收尾时 | 待定。需在"定义最小状态"与"明确写进 §3.2 不做"之间二选一，**不允许继续空着** |

### 3.4 范围变更流程

```
新需求出现
   ↓
是否在 §3.1 范围内？
   ├─ 是 → 进 §5 任务明细，按当前里程碑排期
   ├─ 否 → 进 §3.3 待定区 → 标注决策截止点 → 到期评审
   └─ 与 §1.3 铁律冲突 → 触发 P0 评审（严肃对话，不是顺手改）
```

---

## 4. 里程碑与阶段计划

> **不设日期，只设"做完了才能进下一步"的验收标准。**
> 这类工具死在"功能都做了一半"的概率，远高于死于做得慢。

### M0 · 地基验证 ⚠️ **可判定部分已完成，收尾卡在验收 6 ＋ B-07 拖动缺陷（2026-09-22）**

**目标**：把最大的技术不确定性一次性打掉。

**结论：打掉了** —— **验收 1（窗口渲染 + 尺寸精确）与验收 2（`backdrop-filter` 像素级确证生效）这两条决定性的可行性项都已通过**，技术可行性成立。

> ⚠️ **但 M0 尚未收尾**：4 项人工判定已于 2026-09-20 出结果（**2 通过 / 1 不通过 / 1 待重验**），
> 其中**验收 6 不通过**。而 M1 的前置依赖写的是"M0 **全部通过**"（见下方 M1 节），
> 所以在验收 6 处置完之前，**M0 不进入收尾状态**。处置口径见本表「验收 6 不通过怎么办」。

| 项 | 内容 |
|---|---|
| 交付物 | `m0-probe/` 验证工程（可 `npm run selftest` 一键自检并出报告） |
| 验收标准 1 | Electron 无边框透明**置底**窗口在 Windows 上正常渲染（**2026-09-22 依 ADR-012 改定语**；原文写"置顶"） |
| 验收标准 2 | `backdrop-filter` 在透明窗口内**确实生效**（不是静默失败） |
| 验收标准 3 | 窗口**不抢焦点**（正在打字时不会被抢走输入） |
| 验收标准 4 | 全屏应用时窗口自动隐去（**2026-09-22 依 ADR-012 命题恢复有效**：卡片重回普通顶层窗口，"让位"重新是个有意义的命题） |
| 验收标准 5 | 三档透明度在**纯白 / 纯黑 / 花哨风景**三张壁纸上对比度均 ≥ 4.5:1 |
| 验收标准 6 | 在 100% / 125% / 150% 三档缩放的**单屏**环境下，卡片物理尺寸保持一致、文字清晰不模糊 |
| 验收标准 7 | 在**双屏混合缩放**环境（如 4K@150% + 1080p@100%）下，卡片在主屏与副屏**均不出现模糊或拉伸** |
| 验收标准 8 | 卡片从主屏拖到副屏（含反向）后，**吸附位置与记忆坐标均正确**，偏差 ≤ 5px |
| 验收标准 9 | 确认 `scaleFactor` 的可靠读取方式（若 `getPrimaryDisplay()` 不可靠，改用 `getDisplayNearestPoint()` 或在渲染进程侧用 `window.devicePixelRatio` 交叉校验） |
| 前置依赖 | 无 |
| **状态** | **✅ 通过 6 / ❌ 不通过 1 / ⚠️ 有保留 0 / ⛔ 无法验证 1 / ⬜ 未判定 1**（2026-09-20 回填；不通过项 = 验收 6，未判定项 = 验收 8） |
| 验收记录 | `docs/05-M0验收记录.md`（**结论，v1.3.0**）＋ `m0-probe/report/_selftest-report.md`（**证据**，✅ 已于 2026-09-20 **11:03:42** 重建，摘要 `4/0/0/1/4`）＋ `m0-probe/report/M0-验收记录_2026-09-20_03-12-11.md`（**人工判定的唯一证据** —— 人工结论不持久化，见 `docs/05` §0.2）。污染版仍留档为 `_selftest-report.calib.md`，**勿当验收记录用** |
| 剩余动作 | ① ✅ **人工判定 4 项已完成**（验收 4/5 通过、验收 6 不通过、验收 8 待重验）；② 🔴 **补验收 6 的三档读数取证**（方案见 `docs/05` §5.1）—— **这是当前唯一的收尾阻塞项**；③ 实跑验证新增的拖动把手 `.grip`；④ 接入第二块显示器后补测验收 7 |
| 失败预案 | ① 若 `backdrop-filter` 不可用（R8）→ 切换为不透明降级模式（ADR-008），**只需改一个默认值，不需重审 §02**；② 若验收 7 无法在合理成本内解决（B1）→ 同上降级，并明确将"混合缩放下的副屏"列为已知限制 |
| **验收 6 不通过怎么办** | ⚠️ **第一步不是选处置方案，而是补读数定位。** 现状：本表「失败预案」只列两条触发线（**R8** 与**验收 7**），**验收 6 不在其中**；但 §8 的 **R9** 覆盖验收 6/7/8/9，且写着"若不可解 → 触发 ADR-008 降级" —— **两处口径需要对齐，而对齐的前提是先知道根因**：① 若根因是**几何**（DIP 尺寸不一致 / 坐标偏移）→ **降级无用**，必须修缩放适配；② 若根因是**渲染**（文字发虚 / 重采样）→ 降级**可能**有效，但仍应先试更精准的修法（字体渲染 / 合成层）。**⇒ 先补三档读数取证（`docs/05` §5.1），再据此定路径并对齐两处口径** |

**M0 的额外产出**：探针在实施过程中抓出 **5 个静默失败级问题**（B8–B12，明细见 `docs/04-项目审查报告.md` §6），
其中 2 项（B10 顶层 await 死锁、B11 环境污染导致 Electron 退化为 Node）已**固化为代码层强制约定**，不依赖人记得。

> **2026-09-18 进度更新**：验收 2 与验收 3 已由"待人工"转为**纯程序判定并通过**（`docs/05` §6.5 第 1、2 步落地）。
> 摘要随之由 `2/0/1/1/5` 变为 `4/0/0/1/4`。
>
> ✅ **2026-09-20 更新：验收 3 的裁判校准已完成。** 2026-09-19 **11:20** 第 3 次执行 `npm run calibrate`，
> 校准器报 **`CALIBRATED · 裁判有效`** —— 植入 `show()` 后验收 3 **确实报 `fail`**（`cardFocusCount=1`）。
> 所以验收 3 现在的"通过"含义是"**判据成立，且该裁判已被证明能识别出坏情况**"。
> （此前两次尝试 09:37 / 10:13 被**环境侧 GPU 崩溃**阻断、被正确判为 `INCONCLUSIVE` —— 成因是**环境态**，非本机永久故障。）
> ⚠️ 同一轮还暴露并修掉了校准器的一个**留痕污染缺陷**（它内部那次自检会覆盖正式验收记录），详见 `docs/05` §1 验收 3。
>
> ⚠️ **2026-09-20 更新：4 项人工判定出结果，M0 首次出现 ❌ 不通过。**
> ① **验收 4 ✅ 通过** —— 卡片**盖不住**全屏应用（全屏应用在上层）→ **Windows 自己会处理，M1 不需要额外的"主动让位"实现**。
> ② **验收 5 ✅ 通过** —— 3 壁纸 × 3 档透明度 **9 组全部**读得清（含最通透的 86% 档），**档位不需要收紧**。
> ③ **验收 6 ❌ 不通过** —— **100% 与 125% 两档都有问题**（150% 档读数正常）。
>    ⚠️ 那两档读数已因产物同名而**被覆盖**，所以**当前只有口述、没有可复核的证据**；
>    **取证方案见 `docs/05` §5.1**。**它不触发上面的降级预案**（理由见本表新增行）。
> ④ **验收 8 ⬜ 待重验** —— 实测发现**卡片根本拖不动**：核查确认**探针从未实现拖动**（`src/` 内零命中）。
>    已补**显式拖动把手** `.grip`（`-webkit-app-region: drag`），**尚需实跑验证**（含"拖动会不会抢焦点"）。
> ⑤ 同轮还发现**人工结论不持久化**（只写内存、程序一关就丢）—— 这正是那份导出记录成为"唯一证据"的原因。
>
> 摘要随之由 `4/0/0/1/4` 变为 **`6/1/0/1/1`**。

> ### ⚠️ 2026-09-20 追加：需求 1 触发 ADR-005 重审 → M0 的验收口径被改动
>
> 用户提出 5 条新需求（逐条判定见 `§5.3`）。其中**需求 1「卡片只能显示在桌面层，不得遮挡任何应用窗口」
> 与铁律 L5 冲突**（`§3.2` 明确列"桌面壁纸层原生组件（WorkerW）"为不做），按 `§0.3` 触发 **P0 评审**。
>
> **评审结论**：这不是临时翻案 —— `docs/03` 的 **ADR-005**「何时该重审」一栏原文就是
> 「**若用户强烈反映置顶干扰，重新评估**」，**触发条件已成立**。于是走的是一次**预案内的重审**，
> 结论记录为 **ADR-011：卡片窗口层级由「置顶」改为「桌面层」**（`§3.2` 相应翻案，原理由转为 R12）。
>
> **对 M0 的三处影响（必须记账，不能默默消化）**：
>
> 1. **验收 1 的措辞需更新** —— 原文写「透明**置顶**窗口正常渲染」。**技术结论本身不失效**（窗口渲染 + 尺寸精确两条都已被证过），失效的是"置顶"这个定语。
> 2. **验收 4 整体失效** —— 它测的是"全屏应用时卡片让位"。卡片一旦不在上层，**"让位"这个命题就无从谈起**，须重写为"卡片被应用窗口覆盖时的行为"。
> 3. **验收 2 / 6 的结论不可外推** —— 验收 2 证明的是"透明窗口 + `backdrop-filter` 在本机成立"，但**桌面层形态下"下层"的构成不同**（桌面层之上是壁纸与图标，再上才是应用窗口），
>    `backdrop-filter` 到底模糊到什么、是否仍然生效，**完全未验证**；验收 6 的缩放结论同理需在新形态下复核。
>
> **⇒ 因此新增一条 M0 追加验收：**
>
> | 追加验收 | 内容 | 判据 | 状态 |
> |---|---|---|---|
> | **A10** | **桌面层挂载可行性** —— 卡片能否稳定挂到桌面层，且**在新形态下**四件事同时成立 | ① 被任何应用窗口覆盖；② **仍能接收点击**（能展开、能点条目）；③ `backdrop-filter` 仍然生效；④ 三档缩放下尺寸与文字仍正确。**四项各出一条可复现证据；任一不成立即回退 ADR-005（置顶）** | ✅ **已结案（2026-09-22）：判据① FAIL ⇒ 桌面层不可行。经用户裁定改走「置底顶层窗口」（ADR-012），实测可行** |
>
> ⚠️ **为什么它归 M0 而不是 M1**：M0 的目标原文是"**把最大的技术不确定性一次性打掉**"。
> 换窗口层级正是这类不确定性 —— 它可能推翻 ADR-005、废掉需求 2 的拖动、并让验收 2/6 的既有结论失去适用性。
> **把它塞进 M1，等于让整个 M1 建在一个未验证的形态上。**
>
> ---
>
> ### ✅ A10 结论（2026-09-22）—— 桌面层不可行，改走「置底顶层窗口」
>
> **① 桌面层挂载：不可行（判据① FAIL，机器判定）。** 挂进 `Progman` / 它内部 `WorkerW` 的窗口
> **存在、尺寸不变、能被 `WindowFromPoint` 命中（above 时在 DefView 之上），但不会被合成到屏幕上**。
> 四种配置全灭（`progman+layered+above` / `progman+无layered+above` / `workerw+layered+above` / `progman+layered+below`）；
> 每次「同窗口同代码、**尚未挂载**」的**内建正对照**都稳定读到 nearRatio **0.953**，挂载后 **0.491**（= 壁纸基准，增量 **0.000**）。
> ⇒ 既非绘制问题，也不只是 Z 序问题。**机械成因未确证**（推断与 `Progman` 带 `WS_EX_NOREDIRECTIONBITMAP` / DComp 合成有关，**属推测，不是已取证的结论**）。
> ⚠️ **覆盖缺口**：只在本机 **Win11 25H2**（build 26200.9457）验过；**社区 24H2 配方在本机同样不成立 ⇒ 不可外推**。
> **取证产物**：`m0-probe/report/desktop-attach-mount.rev3-{above,below,above-nolayered,workerw}.json`。
> ⚠️ **过程中先出现过一次假读数**：早期脚本全程主线程 `Start-Sleep`、从不抽消息，而系统 `Button` 类要靠本线程消息泵才能收到 `WM_PAINT`
> ⇒ "窗口存在、能被命中、却看不见"是**必然的**，与桌面层无关（一度被判为 UNABLE，正确）。
> **⇒ 教训：判"人能不能看见"这类事，必须有机器读数 + 内建正对照，不能只靠肉眼。**
>
> **② 处置：触发 ADR-011 自己预登记的重审条件①**（原文：「若桌面层挂载实测**不可行或不可维护** → 回退 ADR-005（置顶）」）。
> **但用户裁定不走"回退置顶"** —— 那会重新引入"卡片盖住其他应用窗口"这个用户明确反对的行为。
> 改走**第三条路：「置底顶层窗口」** = 不挂桌面层，保持顶层窗口 + `SetWindowPos(HWND_BOTTOM)` + `WS_EX_NOACTIVATE`。
> **实测可行**（`m0-probe/report/desktop-attach-mount.rev4b-bottom.json`，`stage=complete`、`errors` 空）：
> 顶层 Z 序 `ourIndex=419 / progmanIndex=420`（共 421 个顶层窗口）⇒ **紧贴桌面之上、排在其它所有窗口之下**（`gapToDesktop=1`）；
> 判据① **PASS**（power 0.462 / 增量 0.459 / 阈值 0.231）；判据② 代理成立（`hitIsUsOrOurChild=true`）；`sizePreserved=true`。
> ⇒ 决策落为 **ADR-012**（`docs/03`），**ADR-011 加删除线保留**（依 §G.3 精神）。
>
> **③ 它对 M0 的净影响：比 ADR-011 轻得多 —— ADR-011 的四条代价全部撤销**
>
> | 项 | ADR-011（桌面层）下 | **ADR-012（置底）下** |
> |---|---|---|
> | 验收 1 | 「置顶」定语待改 | 定语改为「**置底**」；**技术结论不变** |
> | 验收 4 | ❌ **整体失效**（卡片不在上层，"让位"无从谈起） | ✅ **命题恢复有效** —— 卡片重回普通顶层窗口；且实测已证明"盖不住全屏应用" |
> | 验收 2 / 6 | ⚠️ 结论**不可外推**（下层构成变了） | ✅ **结论可外推** —— 形态与 M0 当时实测的形态一致（普通顶层窗口） |
> | 需求 2 拖动 | ⚠️ 原生 CSS 拖动**必然失效**（ADR-011 代价②） | ✅ **该代价撤销**。⚠️ **但 B-07 是独立缺陷，不因本决策消失**（见 `§5.4` B-07） |
> | **R12**（依赖系统内部结构） | 被接受下来的风险 | ✅ **自然规避**（不再采用 `WorkerW`） |
> | **M0 收尾条件** | 两件：验收 6 取证 ＋ A10 有结论 | **A10 划掉** ⇒ 剩 **验收 6 三档读数取证** ＋ **B-07 拖动缺陷修复** |
>
> ⇒ **净收益**：ADR-012 同时撤销 ADR-011 的四条代价、规避 R12，代价是"**卡片会被任何普通窗口覆盖**"——
> 而这**正是需求 1 要的行为**（"不得遮挡任何应用窗口"）。**换来的代价，恰好就是当初提的需求。**

### M1 · v0.1 能跑

**目标**：自己先用起来。

| 项 | 内容 |
|---|---|
| 交付物 | 可运行的 v0.1：单源 RSS → 去重 → AI 摘要 → 置顶卡片 |
| 验收标准 | **连续三天，每天早上打开电脑就能看到一份自己愿意读的简报** |
| 前置依赖 | M0 全部通过 |
| 状态 | 🟡 **部分交付（2026-09-25）** —— 工程实现已成产品并发布 **v0.1.2**；**交付物四项至此全部落地**（含当天补齐的「AI 摘要」），但验收标准「连续三天」仍无记录、**前置依赖仍未满足**。逐条对照见下 |

**2026-09-25 实况对照（为什么既不是「未开始」也不是「已完成」）**

| 本节的要求 | 实况 | 证据 |
|---|---|---|
| 单源 RSS | ✅ **已远超**：**65 条预置源**（29 条默认开；公网 ＋ 本机 RSSHub 两组），RSS / Atom / RDF / JSON Feed ＋ 站点公开 JSON 接口 ＋ 头条热榜适配器 | `src/ingest/sources.js`、`src/ingest/adapters.js` |
| 去重 | ✅ 已实现（URL 归一 ＋ 标题指纹 → `dedupe_key`；翻页期间新条目既不会漏也不会重） | `src/ingest/urls.js`、§5.2c |
| **AI 摘要** | ✅ **已实现（2026-09-25 当天补齐）** —— 严格按 ADR-004 分两层：第一层只送 id/来源/标题筛出 10 条（便宜），第二层只对入选的这十几条写摘要与总览句（贵）。Key 按 ADR-007 由用户自带、加密存本机（R-E02/R-E05）；模型挂了**照样有内容看**（R-E04，状态如实标 `fallback`）。⚠️ 本条此前判为「未实现 —— 全项目零模型调用」（2026-09-25 全仓检索结论），当天闭环 | `src/shared/ai/`、`src/main/brief-service.js`、`tools/test-ai-brief.mjs` |
| 置顶卡片 | ✅ 已实现，**且层级已演进为「置底」**（ADR-012） | `src/renderer/card.js` |
| 验收标准「连续三天」 | ⬜ **无记录** —— 机器判不了，只能由用户自证（见 §9 M1 专项） | — |
| 前置依赖「M0 全部通过」 | ❌ **未满足** —— M0 仍卡在验收 6（❌）与 B-07（🟡） | §5.1、§5.4 |

> ⚠️ **这是一次显式的过程偏差，如实登记，不抹平。**
> M1 的前置依赖写的是「M0 **全部通过**」，而 M0 至今未收尾。实际发生的是
> **用户在 M0 未收尾的情况下指示继续推进产品工程**，于是 M1 的实现先于 M1 的验收条件完成了。
> **本文件不因此改写前置依赖** —— 改了就等于把偏差洗成合规。此处把它记为一条**已知偏差**：
> **v0.1.2 的发布含义是「功能可用」，不是「按本文件的定义验收通过」。**

### M2 · v0.5 能分享

**目标**：别人也能装。

| 项 | 内容 |
|---|---|
| 交付物 | 安装包 + 首屏示例简报 + 首次启动向导 + 源管理界面 + 定时调度 |
| 验收标准 | **朋友拿到安装包，双击装上，五分钟 + 一次 API Key 配置后看到成品**（已按 ADR-007 放宽；原标准"不需要读任何说明"作废） |
| 关键前置 | ✅ **已决策（ADR-007）**：用户自带 Key，不提供中转。M2 必须交付"首次启动向导 + API Key 一键申请引导" |
| 状态 | 未开始 |

### M3 · v1.0 能留下

**目标**：长期可用。

| 项 | 内容 |
|---|---|
| 交付物 | API 型源接入 + 历史存档回看 + 源健康度监控 + Token 用量统计 |
| 验收标准 | 连续使用两周，没有一次因为"抓取失败/布局问题/太吵"而想关掉它 |
| 状态 | 未开始 |

---

## 5. 当前进度看板

> **每完成一个阶段性任务后必须更新本节。** 这是"进度可见"的唯一来源。

### 5.1 阶段状态总览

| 阶段 | 状态 | 完成度 |
|---|---|---|
| 规划阶段（立项 + 设计规范 + 工程体系） | ✅ 已完成 | 100% |
| M0 地基验证 | ⚠️ **未收尾** —— 9 条验收**全部有明确结论**（含 **1 条 ❌ 不通过（验收 6）**）；**A10 已结案**（桌面层不可行 → ADR-012 置底方案实测可行）；待办 = 验收 6 取证 ＋ B-07 拖动缺陷 | **92%** |
| M1 v0.1 | 🟡 **部分交付** —— 工程实现已成产品并发布 **v0.1.2**；**交付物四项已齐**（含 2026-09-25 补齐的 AI 摘要），但验收标准「连续三天」无记录、**前置依赖（M0 全部通过）未满足** | **67%** |
| M2 v0.5 | ⬜ 未开始 | 0% |
| M3 v1.0 | ⬜ 未开始 | 0% |

> **M0 的完成度怎么算的**：截至 2026-09-20，**9 条验收全部有了明确结论** ——
> **6 通过（1 / 2 / 3 / 4 / 5 / 9）、1 不通过（6）、1 无法验证（7）、1 待重验（8）**。
> 百分比不是重点，**重点是没有一条被含糊带过**。
>
> ⚠️ 但"全部有结论"≠"可以收尾"：**验收 6 不通过**，而 M1 的前置依赖写的是"M0 **全部通过**"。
>
> **2026-09-18**：验收 2 与 3 已由"待人工"转为**纯程序判定并通过**（`docs/05` §6.5 第 1、2 步落地），
> 摘要由 `2/0/1/1/5` 变为 `4/0/0/1/4`。
>
> **2026-09-20**：验收 3 的裁判校准**已完成**（2026-09-19 11:20，`CALIBRATED · 裁判有效`）——
> 至此**本文没有任何"通过但裁判未校准"的项**。同一轮还发现并修掉了校准器的**留痕污染缺陷**
> （它内部那次自检会覆盖 `m0-probe/report/_selftest-report.md`），详见 `docs/05` §1 验收 3。
>
> **2026-09-20（第二次更新）**：**4 项人工判定出结果，M0 首次出现 ❌ 不通过。**
> 验收 4 / 5 → ✅ 通过；**验收 6 → ❌ 不通过**（100% 与 125% 两档有问题，⚠️ 读数待补取证）；
> 验收 8 → ⬜ 待重验（**发现探针从未实现拖动**，已补 `.grip` 把手）。
> 摘要由 `4/0/0/1/4` 变为 **`6/1/0/1/1`**。详见 `docs/05` §0.2 与 §5.1。
>
> **2026-09-22**：**A10 结案 —— 桌面层不可行，改走「置底顶层窗口」（ADR-012），实测可行。**
> 连带效果是 **ADR-011 的四条代价全部撤销**（验收 4 命题恢复、验收 2/6 结论重新可外推、原生拖动前提恢复、R12 自然规避），
> **M0 的收尾条件因此回到"A10 划掉、剩两件"：验收 6 三档读数取证 ＋ B-07 拖动缺陷修复**。
> 详见 `§4` M0 节末「A10 结论」。
>
> **2026-09-25**：**M1 由「未开始」改判为「部分交付」** —— 工程实现已成产品并发布 **v0.1.2**
> （v0.1.0 / 0.1.1 / 0.1.2 三个安装包，见 §5.2c）。**但它没有按 §4 的定义验收通过**：
> 交付物四项里**缺「AI 摘要」**（全项目零模型调用）、验收标准「连续三天」**无记录**、
> 前置依赖「M0 全部通过」**未满足**（M0 仍卡在验收 6 与 B-07）。
> **60% 怎么算的**：交付物 3/4 ＋ 验收标准 0/1 ＋ 前置依赖 0/1 的简单平均。
> **百分比不是重点，重点是没有一条被含糊带过** —— 与 M0 那条同一个口径。
>
> **2026-09-25（第二次更新）**：**「AI 摘要」补齐** —— M1 的交付物四项至此**全部落地**
> （单源 RSS / 去重 / **AI 摘要** / 置顶卡片）。分层调用按 **ADR-004**，Key 按 **ADR-007** 由用户自带、
> 加密存本机（**R-E02 / R-E05**），模型挂了**照样有内容看**（**R-E04**）。
> 完成度算法不变：交付物 4/4 ＋ 验收标准 0/1 ＋ 前置依赖 0/1 ⇒ **67%**。
> ⚠️ **它仍然是「部分交付」** —— 验收标准「连续三天」还是无记录、前置依赖还是没满足。
> **功能齐了不等于这个里程碑验收通过了**，这两件事不能混。

### 5.2 规划阶段已交付

| 交付物 | 路径 | 状态 |
|---|---|---|
| 立项文档 | `docs/01-立项文档.md` | ✅ |
| 设计研究与页面架构规范 | `docs/02-设计研究与页面架构规范.md` | ✅ |
| 工程架构文档 | `docs/03-工程架构文档.md` | ✅ |
| 项目规则 | `项目规则.md` | ✅ |
| 子代理机制 | `agents/README.md` | ✅ |
| 同步检查工具 | `tools/sync-check.mjs` | ✅ |
| 项目审查报告（首轮，40 项缺口） | `docs/04-项目审查报告.md` | ✅ |

### 5.2b M0 阶段已交付（2026-09-18）

| 交付物 | 路径 | 状态 |
|---|---|---|
| **M0 验收记录**（结论） | `docs/05-M0验收记录.md` | ✅ |
| M0 探针工程 | `m0-probe/` | ✅ |
| ├ 探针自检原始报告（证据） | `m0-probe/report/_selftest-report.md` | ✅ |
| ├ Electron 唯一启动入口 + 开机心跳 | `m0-probe/tools/launch-electron.mjs` | ✅ |
| ├ Windows 独立裁判脚本（DPI 校验） | `m0-probe/tools/win-display-facts.ps1` | ✅ |
| └ 取证实验（5 个，含原始输出） | `m0-probe/experiments/` + `m0-probe/report/*.txt` | ✅ |

### 5.2c M1 阶段已交付（2026-09-25 补记 —— 作为 v0.1.x 发布）

> ⚠️ **列在这里的是「已经交付的东西」，不是「M1 已完成」** —— M1 的定义见 §4，它**尚未达成**（缺 AI 摘要）。

| 交付物 | 路径 / 证据 | 状态 |
|---|---|---|
| **v0.1.0 / 0.1.1 / 0.1.2 三个安装包** | GitHub Releases（`LiujYi0425/morning-brief`） | ✅ |
| 抓取层（RSS / Atom / RDF / JSON Feed ＋ 站点 JSON ＋ 头条热榜） | `src/ingest/`（手写解析器，**零运行时依赖**） | ✅ |
| **五个维度的类别筛选栏**（代码里 28 类；新装机 22 类，另 6 类随本机源启用而出现） | `src/ingest/sources.js`、`src/renderer/card.js`；`README.md` 有专节 | ✅ |
| 源管理（增 / 删 / 启停 / 本机源批量开关） | `src/store/db.js`、`src/main/ipc.js` | ✅ |
| 定时调度 ＋ **错过补抓** | `src/main/scheduler.js`（`catchUpDecision` 是纯函数，可离线穷举） | ✅ |
| 后台运行 / 停止 / 状态 / 日志（四条命令） | `tools/service.mjs` | ✅ |
| 自更新（两段式）＋ **失败自动回退** | `src/shared/update.js`（快照 ＋ 健康判定） | ✅ |
| 源健康度（正常 / 未跑 / 异常） | `src/renderer/card.js` | ✅ |
| **离线考裁判**：198 条断言 ＋ 44 个变异体 ＋ 打包隐私门 | `tools/test-all.mjs`、`tools/test-mutants.mjs`、`tools/check-dist.mjs` | ✅ |
| **AI 摘要**（分层调用，ADR-004） | `src/shared/ai/`（prompt / parse / plan / client）、`src/main/brief-service.js` | ✅ |
| **API Key 的加密存储**（R-E02 / R-E05） | `src/main/keystore.js` —— `safeStorage`，密文落 `userData`，**不进库、不进日志、没有读回明文的通道** | ✅ |
| 简报的界面落点 | `#headline`（总览一句话）＋ 条目摘要行 ＋ ⚙ 设置面板（`src/renderer/`） | ✅ |
| AI 层的离线考裁判 | `tools/test-ai-brief.mjs`（**18 条断言**，假 client ＋ 临时库，**不联网、不花钱**）＋ **4 个变异体** | ✅ |

### 5.3 当前任务明细

**M0 任务（未收尾 —— 验收 6 待取证 ＋ B-07 拖动缺陷待修）**

| 任务 | 状态 | 备注 |
|---|---|---|
| 搭建 Electron 最小验证工程 | ✅ 已完成 | `m0-probe/`，`npm run selftest` 一键自检 |
| 验证透明窗口渲染 | ✅ **通过** | 验收 1，尺寸 360×44 精确（曾经的 FAIL 见 B12） |
| 验证 `backdrop-filter` 生效 | ✅ **通过** | 验收 2，像素级测量 + 内建校准（A 区方差 `0.0` vs 对照组 `8646.9`） |
| 验证不抢焦点 | ✅ **通过** | 验收 3，程序自动判定；**裁判已于 2026-09-19 校准为 `CALIBRATED`** |
| 验证全屏自动隐去 | ✅ **通过**（人工，2026-09-20） | 验收 4，卡片**盖不住**全屏 → **M1 不需要额外工作** |
| 三张壁纸对比度验证 | ✅ **通过**（人工，2026-09-20） | 验收 5，**9 组全部**读得清（含 86% 通透档） |
| 三档缩放单屏尺寸一致性验证 | ❌ **不通过**（人工，2026-09-20） | 验收 6，**100% 与 125% 两档都有问题**；⚠️ **读数待补取证**（见 `docs/05` §5.1）—— **当前唯一的收尾阻塞项** |
| 双屏混合缩放渲染验证 | ⛔ **无法验证** | 验收 7，**本机只有 1 块显示器**（已显式留痕，非跳过） |
| 跨屏拖动坐标记忆验证 | ⬜ **待重验** | 验收 8，自动部分已通过（往返 `stable=true`）；**手拖部分原本拖不动**（工程从未实现拖动），已补 `.grip` 把手，待实跑 |
| `scaleFactor` 可靠读取方式确认 | ✅ **通过** | 验收 9，两条独立证据链交叉校验吻合 |
| **新发现的问题处置（B8–B13）** | ✅ 全部完成 | 见 `docs/04-项目审查报告.md` §6（B13 已于 2026-09-19 闭环） |
| **A10 桌面层挂载可行性**（追加验收） | ✅ **已结案（2026-09-22）** | 判据① **FAIL** ⇒ 桌面层不可行；经用户裁定改走 **ADR-012「置底顶层窗口」**，**实测可行**（可见 ＋ 可点 ＋ 紧贴桌面之上）。详见 `§4` M0 节末「A10 结论」 |

**M1 前置任务（下一步）** — 详见 `docs/04-项目审查报告.md` §5 第二批

| 任务 | 状态 | 备注 |
|---|---|---|
| B2 Electron 安全基线（搬运 + 补全） | ⏳ 初版已在 M0 落地 | 单实例锁 + 三重导航拦截 + 隔离配置 |
| D1 单实例锁（继承） | ⏳ 初版已在 M0 落地 | `requestSingleInstanceLock` + `second-instance` 唤出 |
| A2 补跑机制 | ✅ **已完成**（2026-09-25 复核） | `src/main/scheduler.js` 的 `catchUpDecision`（纯函数、可离线穷举）：错过时间点后按 `minGapMs` 防抖补抓 |
| D2 离线行为 | ✅ **已完成**（2026-09-25 复核） | 全部源失败时 10 → 30 → 60 分钟退避重试，且卡片**如实说明**「今天还没抓到，正在自动重试」，不拿旧闻冒充今天 |
| B3 日志与可观测性 | ✅ **已完成**（2026-09-25 复核） | `src/shared/run-log.js`（纯 Node、可离线调用）＋ `src/main/crashlog.js` ＋ 开机心跳 ＋ 界面上的源健康度（正常 / 未跑 / 异常 三类如实显示）。**B13 的「失败必须留痕且语义明确」已成立**，完整日志体系随产品落地 |
| E1 术语表 | ⬜ **仍未开始** | 成本最低、收益最高。`docs/00-术语表.md` 至今不存在（§10 末「待创建」仍然有效） |
| C1 / C2 lint 与测试策略 | ✅ **已完成**（2026-09-25 复核） | 落成了比 lint 更强的东西：**离线考裁判**（`npm test` 198 条断言 ＋ `npm run test:mutants` 44 个变异体）＋ **打包隐私门** `npm run dist:check`。**「改坏了会被抓住」从约定变成了机器事实** |
| ~~**`sync-check.mjs` 增强：同一份文档的所有「版本载体」必须一致**~~ | ✅ **已落地（2026-09-19），但换了方案** | 原方案是"逐载体比版本号"（① §7 变更记录里的目标版本 ② §6 同步矩阵 ③ 代理页脚，三者各自 vs frontmatter）。**实际落地时换成了更根本的一招** —— 不再去追三处副本，而是**改查「核对留痕」**：`depends_on: ID@V` 的字面量必须出现在该文档的变更记录（代理则在 `Lessons` 或 `agents/README.md` §7）。**理由**：追副本是打地鼠（每加一个版本载体就多一个补丁），而留痕检查把"下游真的看过上游了"这件事变成可机检的事实，**三处副本即使漂移，也骗不过留痕检查**（§6 那处副本另由**第 8 项检查**看着，见下一行）。详见 §5.5 机制行与 §7「C+E 已上线」 |
| **`sync-check.mjs` 新增三项检查：核对留痕 + 表格完整性 + §6 版本表** | ✅ **已落地（2026-09-19）** | ① **第 6 项 · 核对留痕**（`R-D03` 的机器化）：三道闸 —— 依赖必须存在 / 声明版本必须等于上游当前版本（否则 `[未跟进]`）/ 必须留下含**确切版本号**的核对痕迹（否则 `[缺留痕]`）；**frontmatter 与页脚一律不算留痕**。② **第 7 项 · 表格完整性**：某行以 `\|` 开头、上面隔着空行最近的非空行也是 `\|` 开头 → 报警（后跟分隔行则视为新表头，不报）。③ **第 8 项 · §6 同步矩阵版本 vs frontmatter**（**WARN，不是 FAIL**）—— 因为 §6 现已是全库**唯一的权威版本表**（根 `README.md` 已把副本删掉、改为指向它），它一旦落后就会误导读者，而此前**没有任何检查在看着它**；故意只报 WARN，是因为"**攒批**"时 §6 落后是**有意**的（`docs/05` §7 有历史记录），不该被判成错误，但必须浮出来。④ 另加一道**防静默失效闸**：留痕位置取到**空内容** → 直接报错（否则整条第 6 项检查会安静地失效）。**三项新检查全部已考裁判**：第 8 项做了**变异测试**（把 §6 的 `PROJECT-RULES` 行改成 1.3.0 → 当场报出预期 WARN，位置与措辞均正确）；第 7 项一次抓出**全库 7 处断表**（`docs/01` L322 / `docs/02` L754 / `docs/04` L295 / `docs/05` L521、L523 / `项目规则.md` L462 / 本文件 L412）并全部修复；第 6 项在修掉页脚漏洞后，真实缺留痕从 2 项跳到 **8 项** |
| **候选规则 `R-B12`（原记作 R-B10，已改号）「同一份受管文件必须整块一致修改」** | ⏳ 已登记待走流程<br>（**已有 1 个实证案例**） | 拟入 `项目规则.md`：frontmatter 与正文同一次改完、`depends_on` 与 `version` 同一次改完、**页脚与 frontmatter 同一次改完**，收尾以 `sync-check` 为准。**属实质变更，须走流程**，不在收尾时顺手改。✅ **2026-09-19 拿到第一个真实案例**：四个代理定义页脚版本号漂移（`docs/05` §7.3）。📌 改号原因：`R-B10` 已正式签发（主进程失败语义），候选者让号 —— **先签发的先占号** |
| `design-critic` 增设「第六组 · 平台硬规则」 | ⏳ 已登记待走流程 | 覆盖 B10/B11/B12 **＋ 新增的 §5.8 / R-B11（拆零依赖模块）**。属**代理定义实质变更**，须走 curator 流程（见 `agents/README.md` §7 与 `registry.json` 的 `pending` 字段）。**注意：本轮 `design-critic` 已因 S-01 措辞升到 1.2.0，这一组应紧随其后** |

**2026-09-20 登记的新需求（用户一次提出 5 条，按 §0.3 逐条判定）**

> 判定依据 `§0.3` / `§3.4`：在 `§3.1` 范围内 → 进本表；不在 → `§3.3` 待定区；**与铁律冲突 → P0 评审**。
> **登记不等于承诺** —— 下表"状态"一栏说的是**现在到哪一步了**，不是"已经答应做"。

| # | 需求 | 在 §3.1 范围内？ | 与铁律冲突？ | 判定与去向 | 状态 |
|---|---|---|---|---|---|
| 1 | **窗口层级**：卡片只能显示在桌面层；其他应用窗口激活/打开时不得遮挡其页面，须保持在应用窗口下方 | 边缘 —— 属「展示 → 常驻桌面卡片」的属性，但层级本身就是产品定位 | ⚠️ **是** —— `§3.2` 明确列 WorkerW 为不做，撞 **L5「边界清晰」** | **触发 P0 评审**。结案：**ADR-011 改为桌面层** + **ADR-005 重审结案** + **M0 追加验收 A10**（桌面层挂载可行性）。**2026-09-22 二次结案**：A10 判据① FAIL（桌面层不可行）→ 触发 ADR-011 预登记的重审条件① → 经用户裁定改走**「置底顶层窗口」＝ ADR-012**，**实测可行**。⚠️ **ADR-011 的连带后果随之一并撤销**：`-webkit-app-region: drag` 的适用前提**恢复**（但 B-07 是独立缺陷，见 `§5.4`）；验收 1 定语改为「置底」、**验收 4 命题恢复有效**、验收 2/6 结论**重新可外推**；`§3.2` 的 WorkerW 边界**回归"不做"**（R12 自然规避） | ✅ **已决策（ADR-012）**；⬜ **实现未开始**（当前探针仍是 `alwaysOnTop: true` 的普通顶层窗口，**尚未置底**） |
| 2 | **拖动与固定**：自由拖动 / 固定两种模式，拖到位后可随时切为固定 | ✅ 是（常驻卡片的交互） | 否 | 进 M1。位置记忆**已有一半**：`contract.cjs` 有 `WINDOW_RECORD_POS` 通道、`window.js` 会读 `state.posMemory`；**"固定"是全新状态**，需落盘 + 启动恢复 + 卡片上一个切换入口（现在没有）。**用户裁定"固定"=「只锁坐标」**（明确不是"锁坐标 + 置顶"）。⚠️ **2026-09-22 更新（ADR-012）**：ADR-011 曾使"原生拖动必然失效"，**该前提已撤销**（卡片重回普通顶层窗口）。**但 B-07 是独立缺陷** —— 本机实测 `-webkit-app-region: drag` 在当前窗口配置下**确实不生效**，与层级无关。⇒ 需求 2 落在 B-07 的处置上：**仍建议整体换 JS 手动拖动**（一次同时解决 B-07 ＋ 需求 2），但**不再是"被迫"** | ⬜ **待 B-07 诊断结论后实现** |
| 3 | **资讯类别筛选**：顶部提供可自定义的类别筛选，不采用固定模板/固定形式 | ✅ 是（展示 + 管理） | 否 | 进 M1。⚠️ **净新增界面结构** —— 现在卡片顶部**没有任何筛选栏**，三个分组（`card.html` 的"AI 与算力 / 行业动态 / 你订阅的博客"）都是写死的假数据。按 **R-A03** 必须先出方案再写代码，且需同步 `docs/02`。**用户裁定类别来源 =「用户自定义固定类别」**（自己增删改名称与关键词/来源）—— 明确**否掉了"AI 每日动态生成"**那条路，因为它撞 `§3.2` 列为反模式的「AI 自适应界面（动态重排布局）」与铁律 L2 | ⬜ 待开始 |
| 4 | **资讯链接跳转**：每条资讯可点击并跳转到对应网址 | ✅ 是（展示） | 否 | 进 M1。**`docs/02` §4.3 早就设计了它**（`ItemRow` 说明原文："hover 显示原文链接"），只是停在提示、一直没做跳转。落点三件事：① `contract.cjs` 词汇表新增一条通道；② 主进程用 `shell.openExternal` 打开；③ **必须做协议白名单（只放 http/https）**。⚠️ 卡片当前 CSP 是 `default-src 'none'`、**零外联能力** —— 这个状态是对的，**不要顺手放开**。另需给数据模型加 `url` 字段 | ⬜ 待开始 |
| 5 | **移除「展开全部」**（称其"存在问题且无实际必要"） | ✅ 是（展示） | 否 | **已实现（2026-09-20）**：`card.html` 删除 `#btn-all`；`card.js` 占位循环由三个减为两个。**核查确认用户的判断成立** —— 它在 M0 里就是个空壳（点击只打一行日志，从未有过真实行为）。⚠️ **但它不只活在代码里**：`docs/01` §四 线框图、`docs/02` §4.2 布局图、§4.3 `CardFooter` 组件表**三处都记着它**，按 **R-D04（文档优先于代码）** 必须一并同步 → `CHARTER` 1.1.1→**1.2.0**、`DESIGN-SPEC` 1.1.2→**1.2.0** | ✅ **已完成（含文档级联）** |

### 5.4 阻塞项与风险

| 编号 | 描述 | 影响 | 状态 |
|---|---|---|---|
| B-01 | `backdrop-filter` 在 Electron 透明窗口中可能静默失效 | 会推翻整套"玻璃功能层"方案 | 🟢 **已闭环（2026-09-18）**：验收 2 已由**像素级测量**自动判定通过（正常态 A 区方差 `0.0` vs 对照区 `8646.9`；校准态两区 `8646.9 / 8646.9`、比值 `1.000`），且**校准态每次自检都自动跑**。降级预案仍是一等公民，但**未被触发** |
| B-05 | **验收 6（三档缩放下尺寸一致 / 文字清晰）不通过** | **M0 无法收尾 → M1 前置依赖不满足** | 🔴 **新增（2026-09-20）**：人工实测 **100% 与 125% 两档都有问题**。⚠️ **那两档读数已因产物同名被覆盖** → **必须先补取证再定位**（`docs/05` §5.1）。**不触发 §4 降级预案** —— 预案只对应 R8 与验收 7，而这是**几何**问题，降级为 `opaque` 对它无效 |
| B-02 | 透明无边框窗口 + 多显示器混合缩放存在 DPI 缺陷 | 双屏用户可能遇到模糊、拉伸、坐标偏移 | 🟢 **大幅降级**：M0 实测证明 `scaleFactor` 在本机可靠（验收 9 通过），且"尺寸偏差"的真实成因是**坐标物理像素对齐**（B12），**单屏上就已复现并解决**，与是否多屏无关。剩余未验证面：双屏混合缩放本身（本机无第二块屏） |
| B-03 | **Electron 在特定环境下会静默退化**（B10 顶层 await 死锁 / B11 `ELECTRON_RUN_AS_NODE` 环境污染） | 开发与自检可能"跑了但什么都没发生"，且报错信息误导 | 🟢 **已解决**：唯一启动入口 + 开机心跳，让静默失败变成可判定报警。已升格为 `项目规则.md` 强制条款 |
| B-04 | 窗口尺寸/坐标存在 1px 级平台规则（B12） | 尺寸会悄悄错 1~4px，视觉上不明显但违反"精确"验收 | 🟢 **已解决**：`setBounds` 校正 + 坐标网格对齐，已接入初始定位/吸附/换形态/跨屏移动四条路径 |
| B-06 | **ADR-011（改为桌面层）使 M0 的部分验收结论失去适用性，且新形态完全未验证** | 若桌面层挂载不可行 → ADR-011 须回退；若可行 → 验收 1 措辞更新、**验收 4 重写**、验收 2/6 复核都要落地 | 🟢 **已闭环（2026-09-22）**：A10 判定桌面层**不可行**（判据① FAIL），改走 **ADR-012「置底顶层窗口」**（实测可行）⇒ **本风险描述的四种"失去适用性"影响全部撤销**（验收 4 命题恢复、验收 1 定语改为「置底」、验收 2/6 结论重新可外推）。**它当初的警示是对的** —— 换层级确实会牵动验收口径；只是最终换的是**另一条路** |
| B-07 | **卡片拖动把手实测无效（`-webkit-app-region: drag` 在该窗口配置下不生效）** | 验收 8 的手动部分**至今无法执行**。⚠️ **2026-09-22 更正一处牵连**：此前把它与"ADR-011 迁到桌面层后该属性必然失效"并列，**但那是另一件事** —— ADR-012 已撤销桌面层，**本缺陷与层级无关**，是窗口自身配置的问题 | 🟡 **已定位、未修**：本机已验证"`card.js` 里没有任何 JS 拖动代码、拖动唯一依赖 CSS"；外部有可直接对照的 A/B（把 `resizable` 改回 `true` 即恢复可拖）→ **高置信推论**为 `resizable: false`。**已建独立诊断开关 `M0_DIAG_RESIZABLE`**（零依赖模块 `diag-flags.js` ＋ 离线考裁判 `npm run test:flags`）待实测取证。**修法方向已定：整体替换为 JS 手动拖动（mousedown → IPC → setBounds）** —— 一次同时解决本缺陷与需求 2「锁定坐标」 |

### 5.5 待决策与已决策

| 编号 | 事项 | 状态 | 结论 / 载体 |
|---|---|---|---|
| A1 | API Key 方案（原风险 R6） | ✅ 已闭环 | 纯本地 · 用户自带 Key，不提供中转（ADR-007） |
| B1 | 透明窗口 + 混合 DPI 缺陷 | ✅ 已闭环 | 完整覆盖 + 可切换降级；不透明降级为一等公民（ADR-008） |
| B12 | 窗口尺寸与坐标的平台硬规则 | ✅ 已决策 | **构造参数尺寸不可信，必须 `setBounds` 校正；坐标必须落在物理像素网格上**（ADR-009 / R-B08） |
| B11 | Electron 启动方式 | ✅ 已决策 | **唯一启动入口 `tools/launch-electron.mjs`**，负责净化环境 + 守护开机心跳（R-B09） |
| B10 | 主进程异步写法 | ✅ 已决策 | **ESM 主进程禁止顶层 `await app.whenReady()`**，一律 `.then()`（R-B07） |
| B13 | 主进程失败语义（`uncaughtException` 该不该退出） | ✅ 已决策 | **落盘错误上下文 + 明确退出策略**：`uncaughtException` → 同步落盘 + `process.exit(1)`；`unhandledRejection` → 落盘不退出（**R-B10**）。与 B3 合并处置 |
| R-B12 候选 | 「同一份受管文件必须整块一致修改」是否立规 | 🅿️ 已登记待定 | 已有 1 个实证案例（页脚版本漂移）。**属 `PROJECT-RULES` 实质变更，须走流程** |
| 机制 | 「记账式改动是否升版」—— 方案 A / B / C / E | ✅ **已决策（2026-09-19）** | **采纳我推荐的 C+E 合并形态** —— 用户拍板原话：「按照你推荐的方案进行」。**版本号只表示"这份文档自己变了"；核对义务改由「留痕」表达。** 三个载体：① `项目规则.md` **R-D05**（记账 vs 实质的分界线）；② **R-D03 改写**（留痕必须含**确切版本号字面量**）；③ `tools/sync-check.mjs` **第 6 项检查**（改查留痕，不再逐个比对三处副本）。**推论：级联只跑一轮。** 方案 A / B **未采纳** —— 它们要求每份文档声明"读了上游哪一部分"，而实证（本次 1 真消费者 vs 7 记账）说明这种粒度不会被认真维护。详见 §7「C+E 已上线」 |
| A3 | 「已读 / 未读」状态 | 🅿️ 已登记待定 | §3.3 待定区，M1 收尾前必须二选一 |
| **层级** | **卡片窗口层级：置顶 vs 桌面层 vs 置底** | ✅ **已决策（2026-09-22）** | **「置底顶层窗口」（ADR-012）** —— 保持普通顶层窗口 ＋ `SetWindowPos(HWND_BOTTOM)` ＋ `WS_EX_NOACTIVATE`，视觉上等同"贴在桌面上"。**完整决策链**：置顶（ADR-005）→ 桌面层（ADR-011，2026-09-20）→ **置底（ADR-012，2026-09-22）**。**为什么不是"回退置顶"**：A10 确实触发了 ADR-011 预登记的重审条件①，但回退置顶会**重新引入"卡片盖住其他应用窗口"**这个用户明确反对的行为 ⇒ 走第三条路。**实测依据**：`m0-probe/report/desktop-attach-mount.rev4b-bottom.json`。⚠️ **本行是补齐的** —— 1.3.0 的变更记录声称"§5.5 +层级决策"，但当时**从未落笔**，详见 `§7`「三处留痕纠错」 |

---

## 6. 文档同步矩阵 ⭐

> **这是四份文档保持一致的机械保证。**
> 每次修改任一文档后，运行 `node tools/sync-check.mjs` 校验。
> 校验不通过 → 说明有文档没同步，**先修同步，再继续开发**。

| doc_id | 文档 | 路径 | 当前版本 | 依赖 | 上游需同步 | 状态 |
|---|---|---|---|---|---|---|
| `MASTER-PLAN` | 项目计划工程书 | `项目计划工程书.md` | **1.6.0** | — | — | ✅ SSOT。**2026-09-25 第 4 次实质升版（P2）** —— **「AI 摘要」补齐 ⇒ M1 交付物四项全齐**（`§4` 实况对照表、`§5.1` 60% → **67%**、`§5.2c` ＋4 行、`§9` M1 专项两条改成「可测、待真机回填」）。**级别定 P2**：它是**执行**（把 M1 定义里本来就有的东西做出来），不是改里程碑边界 ⇒ 非 P1；但改到了 §4/§5 的正文 ⇒ 非纯记账。此前一次为 **2026-09-25：第 3 次实质升版（P1）** —— M1 由「未开始」改判为 **🟡 部分交付**（v0.1.2 已发布，但缺 AI 摘要、验收标准无记录、前置依赖未满足）：`§4` M1 节重写状态并加实况对照表、`§5.1` 阶段总览、`§5.2c` 新增 M1 已交付清单、`§5.3` M1 前置任务复核、`§9` M1 专项注记、`§10` 项目地图补产品工程树。此前一次为 **2026-09-22：第 2 次实质升版（P1）** —— A10 结案：桌面层**不可行** ⇒ **ADR-012「置底顶层窗口」**（`§3.2` WorkerW 边界回归"不做"、`§4` M0 节末 +「A10 结论」、`§5.3` 需求 1/2 与 +A10 行、`§5.4` B-06 闭环 / B-07 修订、`§5.5` +层级决策行、`§8` +R12（已规避）/ R3 更新、`§9` 收尾清单）。⚠️ **同轮补齐 1.3.0 宣称但从未落地的三处**（`§3.2` 翻案注、`§5.5` 层级行、`§8` R12），详见 `§7`。此前一次为 2026-09-20 第 1 次实质升版（1.2.0→1.3.0） |
| `PROJECT-RULES` | 项目规则 | `项目规则.md` | **1.4.0** | `MASTER-PLAN@1.4.0` | 是 | ✅ **记账核对（2026-09-22）**：核对 `MASTER-PLAN@1.4.0`，**无需改动** —— 层级方案变更走 ADR 与验收流程，**不新增也不改变任何规则**（R12 是风险不是规则）。此前一次为记账核对（2026-09-20）；再前为**实质变更**（**改写 R-D03** 留痕须含确切版本号；**新增 R-D05** 记账 vs 实质；速查表 39 → **40** 条） |
| `CHARTER` | 立项文档 | `docs/01-立项文档.md` | **1.3.0** | `MASTER-PLAN@1.4.0` | 是 | ✅ **实质变更（2026-09-22）**：§三 交互细节「**置顶**但不打扰」→「**不遮挡其他窗口**」；§三「技术实现路径」由"无边框透明**置顶**窗口（`alwaysOnTop: true`）"改为"**置底顶层窗口**（`HWND_BOTTOM`）"；§十三 边界行同步。⚠️ **"卡片会不会遮挡别人"是产品定位的一句话描述，改了它属实质变更**。此前一次为 2026-09-20 移除「展开全部」（实质） |
| `DESIGN-SPEC` | 设计研究与页面架构规范 | `docs/02-设计研究与页面架构规范.md` | **1.5.0** | `MASTER-PLAN@1.4.0` | 是 | ✅ **实质变更（2026-09-22）**：§7「技术侧」自检清单的「Electron 无边框透明**置顶**窗口能正常渲染」→「透明**置底**窗口」（检查标准的措辞变更，与 `design-critic` 那次同类，属实质）。此前一次为 **2026-09-20 第二次实质变更**：① **兑现一次从未落盘的改动** —— 1.2.0 声称改了 2 处（§4.2 布局图 + §4.3 `CardFooter`），实际**只落了 1 处**，`CardFooter` 一直写着「展开全部」；② `CardFooter` 补记三次修订（移除展开全部 / **刷新升级为真实行为** / **设置为 `disabled`**）；③ §5 状态矩阵「三按钮」→「两按钮」；④ **新增 `ToastHint` 组件**。**文档侧根因**：`CardFooter` 从未定义过"点下去该发生什么"，实现于是照着空定义做出了空壳（R-D04）。此前一次为 S-01 措辞更正（禁令针对「硬编码的**值**」，不是属性名） |
| `ARCHITECTURE` | 工程架构文档 | `docs/03-工程架构文档.md` | **1.5.0** | `MASTER-PLAN@1.4.0`, `DESIGN-SPEC@1.4.0` | 是 | ✅ **实质变更（2026-09-22）**：**ADR-011 结案**（加删除线保留，注明被 ADR-012 取代 —— 依 §G.3 精神）＋**新增 ADR-012「卡片窗口层级 = 置底顶层窗口」**；ADR-005 行的"取代链"更新为 005→011→012。此前一次为 2026-09-20（ADR-005 重审结案 ＋ 新增 ADR-011）；再前为 +§5.8 拆零依赖模块、+TD-06 |
| `AUDIT` | 项目审查报告 | `docs/04-项目审查报告.md` | **1.4.0** | `MASTER-PLAN@1.4.0`, `CHARTER@1.3.0`, `DESIGN-SPEC@1.4.0`, `ARCHITECTURE@1.4.0`, `PROJECT-RULES@1.4.0` | 是 | ✅ **记账核对（2026-09-22）**：核对上述四个上游新版本，**结论：本报告内容无需改动** —— 层级方案变更走 ADR（`docs/03`）与里程碑流程，**不新增也不关闭任何 B / G 系列缺口编号**（A10 与 R12 属"验收 / 风险"，不进 §1 总览表）。此前一次为 **记账核对（2026-09-20）**：核对上述四个上游新版本，**结论：本报告内容无需改动** —— 需求 1 的翻案走 ADR（`docs/03`）与里程碑流程，审查报告的 B 系列缺口编号不受影响；需求 5 属设计规范的组件定义变更。此前一次为 B13 闭环、+G4、+TD-06、R-B10/R-B11 登记 |
| **`M0-RECORD`** | **M0 验收记录** | **`docs/05-M0验收记录.md`** | **1.5.0** | `MASTER-PLAN@1.4.0`, `AUDIT@1.4.0`, `ARCHITECTURE@1.4.0`, `DESIGN-SPEC@1.4.0`, `PROJECT-RULES@1.4.0` | 是 | ⚠️ **实质变更（2026-09-22）**：**A10 结论入档** ＋ **ADR-012 下的验收口径改写**（验收 1 定语 →「置底」；**验收 4 命题恢复有效**；验收 2/6 结论**重新可外推**；验收 8 去掉"ADR-011 使 CSS drag 失效"的牵连）。**上一轮登记的"待 A10 有结论后一次性改写"至此兑现**。此前一次为 ⚠️ **实质变更（2026-09-20 第四次）：验收 8 的手拖部分实测仍拖不动 —— 上一轮"已修"结论不成立**，改判为"未判定（诊断中）"，含根因分层结论、不采用 `resizable: true` 当修复的理由、诊断机制与两次运行的操作单；页脚上游版本一并更正。**仍标注待改口径**（ADR-011 使验收 1 措辞待更新、验收 4 整体失效、验收 2/6 结论不可外推，**待 A10 有结论后一次性改写**）。此前为**实质变更（2026-09-20 第二次）：4 项人工判定出结论 → 摘要由 `4/0/0/1/4` 变为 `6/1/0/1/1`，M0 首次出现 ❌ 不通过。** 验收 4 / 5 → 通过；**验收 6 → 不通过**（100% 与 125% 两档有问题，⚠️ 读数因产物同名被覆盖、**待取证**）；验收 8 → 待重验（**发现探针从未实现拖动**，已补 `.grip` 把手）。另新增 §0.2、§5.1，并记录缺陷「人工结论不持久化」。（1.2.2 那次：验收 3 裁判校准完成 `CALIBRATED`） |
| **`M0-PROBE`** | **M0 探针工程说明** | **`m0-probe/README.md`** | **1.8.0** | `MASTER-PLAN@1.4.0`, `ARCHITECTURE@1.4.0`, `M0-RECORD@1.5.0` | 是 | ✅ **实质变更（2026-09-22）**：① **工具清单登记本轮新增的三个 PowerShell 探针**（`desktop-layer-facts.ps1` / `attach-to-desktop-layer.ps1` / `lint-ps1.ps1`）；② **新增「A10 桌面层挂载实验」一节**（六次运行的结论、假读数教训、可复现命令）；③ **拖动一节更新** —— ADR-012 撤销"桌面层使 CSS drag 必然失效"，但 B-07 仍是独立缺陷。此前一次为 ✅ **实质变更（2026-09-20 第四次）**：① 更正上一版"已修"的错误结论（拖动把手实测无效）；② 新增诊断机制（`src/main/diag-flags.js` + `card.js` 判别探针 + `window.js` 无条件留痕 + 离线考裁判 `test-diag-flags.mjs`，`npm test` 三组 → **四组**）；③ 拖动一节重写为「怎么用、为什么拖不动」，含**两次运行的操作单 + 记录表 + 判据表**；④ **修掉卡片死键**（刷新升级为真实行为、设置改 `disabled`、控制台两处补反馈），并把"空壳"与"反馈强度不足"两类刻意分开记。此前为**实质变更（2026-09-20 第二次）：新增卡片拖动能力**（`.grip` 把手）—— 起因是实测发现"卡片根本拖不动"、本工程从未实现拖动。（1.5.0 那次：+零依赖 `artifact-names.js`、+离线考裁判 `npm run test:names`（**3/3 变异体落网**）、校准器加**产物隔离 + 守卫**） |
| `AGENT-SYSTEM` | 子代理机制 | `agents/README.md` | **1.1.2** | `MASTER-PLAN@1.4.0`, `PROJECT-RULES@1.4.0` | 是 | ✅ **记账核对（2026-09-22）**：核对 `MASTER-PLAN@1.4.0`，**无需改动**（层级方案变更不触及任何代理的契约、检查清单或职责边界）。⚠️ **但"`design-critic` 第六组 · 平台硬规则应当包含窗口层级"这一条仍待走 curator 流程**（ADR-012 之后它仍然成立 —— 层级是平台硬规则）。四个代理定义的 `depends_on` 已记账跟进。此前一次为 **记账核对（2026-09-20）**：核对 `MASTER-PLAN@1.3.0`，**无需改动**（本轮不触及任何代理的职责与契约）；四个代理定义的 `depends_on` 已记账跟进（见 `agents/README.md` §7 末）。此前一次为记账核对（+§6.3 版本三载体）；**代理定义「第六组 · 平台硬规则」实质变更仍待办**（见下方说明） |

> **2026-09-25**：`MASTER-PLAN` 升 **1.5.0** ⇒ **8 份下游文档 ＋ 1 个代理定义依 R-D05 记账跟进**（**全部不升版本号**）——
> `PROJECT-RULES` / `CHARTER` / `DESIGN-SPEC` / `ARCHITECTURE` / `AUDIT` / `M0-RECORD` / `M0-PROBE` / `AGENT-SYSTEM`
> 各自把 `depends_on` 更新至 `MASTER-PLAN@1.5.0`，并在自己的变更记录里留下核对痕迹；`curator` 代理定义同步跟进
> （`collector` / `brief-writer` / `design-critic` **不依赖本文件，无需改动**）。
>
> **核对结论：下游内容一律无需改动** —— 本轮改的是**「M1 交付到哪一步」这件事实**
> （里程碑状态改判 ＋ 交付清单归档），**不新增、不删除、不改写任何规则、模块边界与验收口径**。
>
> ⚠️ **但有一个例外必须写下来，不能被「无需改动」这四个字盖住**：
> `CHARTER` 与 `DESIGN-SPEC` 描述的产品能力里含 **AI 摘要**，而它**至今没有实现**。
> **这不是文档要改**（产品定义没变，也不能因为没做就把它从定义里删掉），
> **而是 M1 的缺口** —— 已在 `§4` 与 `§5.1` 如实登记，并随「AI 摘要」一起待做。

> **2026-09-25（第二次）**：`MASTER-PLAN` 再升 **1.6.0**（P2，执行级）⇒ **8 份下游文档 ＋ 1 个代理定义再次依 R-D05 记账跟进**，
> 全部**不升版本号**。**核对结论**：本轮是**把 M1 交付物里缺的那一项做出来**，
> 不新增/不删除任何规则、不改任何模块边界与验收口径 ⇒ **下游内容一律无需改动**。
> ⚠️ 唯独 `docs/03` 是例外：**ADR-004 从「设计」变成了「已落地」**，而且新增了 7 条 IPC 通道
> 与 2 张表的列 —— 那是**架构文档的正文**，必须实改（它自己升 1.5.0）。

> ⚠️ **「上游需同步＝是」这一列的含义，自 2026-09-19 起变了 —— 这是 R-D05 的直接效果：**
>
> - **以前**：它隐含"**要升版**"。于是 `PROJECT-RULES` 升一次 minor，5 个下游各升 patch，**这 5 个的新版本号又让它们的下游过期** —— 级联**递归**。
> - **现在**：它只表示"**要核对，并留下含确切版本号的留痕**"。核对后若内容无需改动，就**记账：改 `depends_on` + 写留痕，版本号不动**。
> - **为什么就终止了**：下游的版本号没变 → **它的下游不会过期** → 一轮到底。本次已实测（见 §7「C+E 已上线」）。
> - **注意**：`depends_on` 从此读作「**我已核对 `ID` 到 `V` 版**」，而不是「我依赖 `ID` 的 `V` 版」。**这一个字的差别，就是整套机制的全部支点。**

**代理定义版本**（不是独立文档，随 `AGENT-SYSTEM` 管理）：`collector` 1.0.3 ｜ `brief-writer` 1.1.2 ｜ `design-critic` **1.2.0** ｜ `curator` 1.1.2 ｜ `arch-critic` DRAFT（未创建）

> **2026-09-22**：四个代理定义的 `depends_on` 依 **R-D05 记账跟进**（**不升版本号**）——
> `collector` / `brief-writer` / `curator` → `ARCHITECTURE@1.4.0` 等；`design-critic` → `DESIGN-SPEC@1.4.0`。
> **核对结论：四者内容均无需改动**（层级方案变更不触及任何代理的输入/输出契约、检查清单或职责边界）。

> **为什么 `M0-PROBE` 也进同步矩阵**：`m0-probe/README.md` 会被 `sync-check` 自动扫到（校验器会遍历根目录下的所有一级子目录）。
> 两条路：把它排除掉，或者把它做成合规文档。**选了后者** —— 它确实需要一份变更记录（M0 期间改动很多），
> 而且它有真实的上游依赖（M0-RECORD、ARCHITECTURE）。**不为了让校验通过而削弱校验器。**

> **`AGENT-SYSTEM` 为何标"待跟进"**：两组变更**确实与 `design-critic` 的检查清单相关** ——
> ① B10/B11/B12 的三条硬规则（第五组"双形态"之后，理应再加一组"平台硬规则"：尺寸精确性、启动方式、坐标网格）；
> ② 2026-09-19 新增的 `§5.8`（拆零依赖模块）与 `R-B11`。
> 但这是**代理定义的实质变更**，按 `agents/README.md` §6 的流程需要走一次 curator 审核，
> 不宜在收尾时顺手改。**已登记为 M1 前置任务**（见 `agents/README.md` §7 与 `registry.json` 的 `pending`），不假装同步完成。
>
> ✅ **但 2026-09-19 这一轮，`design-critic` 确实被实质改了一次** —— 它不是一个"待办"，而是这次级联里**唯一需要改内容的消费者**：
> 第 18 项检查原文抄的是**属性名**（"有没有写 `backdrop-filter`"），而 S-01 禁的是**值**，
> 照字面执行会把 `card.css` 里 5 处合规写法判成 **block**（假阳性）。已改写并升 **1.1.1 → 1.2.0**。

**`CHARTER` 为何只升 patch**：M0 的结论**不改变产品定位与目标用户**——
B10/B11 是开发环境与写法问题，B12 已固化为实现层规则（写进 ADR-009 与项目规则）。
按同步规则第 1 条，核对后内容无需改动，故**只跟进 `depends_on` 并记"已核对，内容未改"**，不为了凑同步而硬改内容。

**`DESIGN-SPEC` 已不再是"仅依赖跟进"**（2026-09-19）：它在 1.1.2 变成了一次**实质变更**——
`§3.7` 与 `S-01` 的措辞原文自相矛盾（写"禁止出现 `backdrop-filter`"，但该文件自己的 `card.css` 里就有 5 处
`backdrop-filter: var(--card-blur)`）。**后果不是"读起来别扭"，而是照字面执行会得出错误结论。**
这次修订的实际影响面已核实：**1 个真消费者（`design-critic`）+ 7 个记账**。


### 同步规则

1. **上游升版本 → 下游必须重新核对。** 核对后若内容无需改动，也要在 §7 记录"已核对，无需改动"。
2. **下游可以独立升级（P2 级）**，但不得与上游冲突。
3. **`MASTER-PLAN` 升到 2.0.0（P0 变更）→ 全部下游强制重审**，并逐个在 §7 留痕。

---

## 7. 变更记录

> **只增不删。** 每一行都是一次决策的存档。

| 日期 | 版本 | 级别 | 变更内容 | 原因 | 影响面 | 同步动作 |
|---|---|---|---|---|---|---|
| 2026-09-25 | **1.6.0** | **P2** | **「AI 摘要」补齐 —— M1 交付物四项全齐，本文件五处改动**：① **§4 M1 节**的实况对照表把「AI 摘要」由 ❌ 改为 ✅（含分层调用、Key 边界、降级三条落地说明），状态行改写；② **§5.1** M1 行 60% → **67%**（交付物 4/4）并补第二次更新说明块；③ **§5.2c** 交付清单 ＋4 行（AI 摘要分层调用 / Key 加密存储 / 界面落点 / AI 层离线考裁判）；④ **§9 M1 专项**两条由「不成立」改为「可测、待真机回填」；⑤ **§6 同步矩阵** ＋ 下游记账核对。**代码侧**：新增 `src/shared/ai/`（prompt / parse / plan / client）、`src/shared/day.js`、`src/shared/keystore-format.js`、`src/main/keystore.js`、`src/main/brief-service.js`；schema **3 → 4**；**7 条新 IPC 通道**；`#headline` 与 ⚙ 设置面板落地 | M1 的交付物里写着「单源 RSS → 去重 → **AI 摘要** → 置顶卡片」，而 2026-09-25 的交付审查发现**「AI 摘要」是全项目唯一没做的那一项**（全仓检索：零模型调用）。用户拍板「下一步就做 M1 缺的那一项」。**依 R-D05 属实质变更**（改 §4 里程碑实况 ＋ 新增交付项）⇒ 1.5.0 → **1.6.0**；**级别定 P2**：它是**执行**（做计划里本来就有的东西），不改里程碑边界与 §3 范围 ⇒ 非 P1 | **0 份实质升版、8 份记账核对**（`PROJECT-RULES` / `CHARTER` / `DESIGN-SPEC` / `AUDIT` / `M0-RECORD` / `M0-PROBE` / `AGENT-SYSTEM` ＋ 代理定义 `curator`）＋ **2 份实质升版**：**`ARCHITECTURE` 1.4.0 → 1.5.0**（ADR-004 从设计变已落地 ＋ 7 条新 IPC 通道 ＋ 数据模型改动，那是架构文档的正文）、**`DESIGN-SPEC` 1.4.0 → 1.5.0**（`#headline` 由占位变真实 ＋ 新增 AI 设置面板组件与条目摘要层级） | `node tools/sync-check.mjs` 复查 —— 全绿（9 份文档 ＋ 4 个代理定义）；`npm test` 三套考裁判（**198 ＋ 46 ＋ 18** 条断言）；变异体全量复跑 |
| 2026-09-25 | **1.5.0** | **P1** | **M1 改判为「部分交付」＋ 产品工程实况入档，本文件七处改动**：① **§4 M1 节**重写状态行并新增**实况对照表**（逐条对「单源 RSS / 去重 / AI 摘要 / 置顶卡片 / 连续三天 / 前置依赖」给出实况与证据）；② **§5.1** 阶段总览 M1 行由「⬜ 未开始 0%」改为「🟡 部分交付 60%」并补说明块；③ **新增 §5.2c「M1 阶段已交付」**清单（三个安装包、抓取层、五维度筛选栏、源管理、调度与补抓、后台运行、自更新与回退、健康度、离线考裁判）；④ **§5.3** M1 前置任务复核（A2 / D2 / B3 / C1-C2 → ✅ 并给出证据，E1 仍 ⬜）；⑤ **§6 同步矩阵**跟进 ＋ 8 份下游记账核对；⑥ **§9 M1 专项**三条补注记；⑦ **§10 项目地图**补产品工程树（`src/` / `tools/` / `package.json`） | 用户问「这程序能不能打包给别人用了」→ 触发一次全面的交付审查。审查把一件**本文件一直没记的事**摆上台面：**v0.1.0 / 0.1.1 / 0.1.2 已经发布，而 §5.1 里 M1 还写着「未开始 0%」**。核查之后**不能简单改成「已完成」** —— M1 的交付物里**「AI 摘要」至今未实现**（全项目零模型调用，经全仓检索确认），验收标准「连续三天」无记录，且**前置依赖「M0 全部通过」未满足**（M0 仍卡在验收 6 与 B-07）。⇒ 如实改判为 **🟡 部分交付**，并把「前置依赖未满足却先做了产品」记为**已知偏差**而不是抹平。**依 R-D05 属实质变更**（改 §4 里程碑状态 ＋ 新增交付清单）⇒ 1.4.0→**1.5.0**；**级别定 P1**（里程碑状态与交付归档，不触及北极星、五条铁律与 §3 范围 ⇒ 非 P0；但改的是里程碑本体 ⇒ 非 P2） | **0 份实质升版、8 份记账核对**（`PROJECT-RULES` / `CHARTER` / `DESIGN-SPEC` / `ARCHITECTURE` / `AUDIT` / `M0-RECORD` / `M0-PROBE` / `AGENT-SYSTEM`）＋ **1 个代理定义跟进**（`curator`；其余三个代理不依赖本文件） | `node tools/sync-check.mjs` 复查 —— 全绿（9 份文档 ＋ 4 个代理定义，0 错误 0 警告） |
| 2026-09-22 | **1.4.0** | **P1** | **A10 结案 ＋ ADR-012 落地，本文件十处改动**：① **§3.2** WorkerW 边界行**补齐翻案与回归的完整链路**；② **§4 M0 节末新增「A10 结论」块**（桌面层不可行 ＋ 置底方案实测可行 ＋ 六项连带影响对照表）并更新追加验收 A10 的状态；③ §4 节标题、验收标准 1 / 4 的定语与命题更新；④ **§5.1** 阶段总览与说明块补 2026-09-22 段；⑤ **§5.3** M0 任务表 +A10 行、需求 1 / 2 行更新；⑥ **§5.4 B-06 闭环**、**B-07 修订**；⑦ **§5.5 新增「层级」决策行**；⑧ **§6 同步矩阵全面跟进**；⑨ **§8 新增 R12（已规避）＋ R3 更新**；⑩ **§9** 收尾清单三项打勾、收尾条件重述。**⑪（同批补充）核实"全绿"时抓到并修掉一处「检查器自身的静默失效」** —— `tools/sync-check.mjs` 的 frontmatter 解析在 **CRLF** 文件上会**丢掉最后一个键**（根因：JS 的 `.` 不匹配 `\r`，而块尾那个孤立 `\r` 既不被 `split(/\r?\n/)` 吃掉、又挡住 `(.*)$`），而本工程当时**唯一**的 CRLF 文件恰好是 `项目规则.md` ⇒ **它的 `depends_on` 从未被机器读到过，"依赖未跟进"这条闸门对它永久失效且不报错**。处置三件：`项目规则.md` 转 **LF**（全项目约定）＋ 解析前**归一化** ＋ **新增「CRLF 直接判 FAIL」**（并已用一次**变异测试**验证新检查确实会报错：造一个 CRLF 探针 → 被点名 → 删除 → 复绿）。⚠️ **同轮更正三处"宣告了没发生的事"**（见下方专门小节） | 阿木在 A10 实测后拍板选 **A · 置底顶层窗口**。**触发依据不是"改主意"，而是 ADR-011 自己预登记的重审条件①**（原文：「若桌面层挂载实测**不可行或不可维护** → 回退 ADR-005（置顶）」）—— A10 判据① FAIL，条件成立。**但结论超出了预案的两条预设出路**：回退置顶会重新引入"卡片盖住其他应用窗口"（用户明确反对），故走第三条路并**新开 ADR-012**。**依 R-D05 属实质变更**（改 §3.2 范围状态 ＋ 四条验收口径 ＋ 新增风险编号）⇒ 1.3.0→**1.4.0**；**级别定 P1**（不触及北极星与五条铁律 ⇒ 非 P0；但改变 §3.2 范围与多条验收口径 ⇒ 非 P2） | **5 份实质升版**（`ARCHITECTURE` 1.3.0→**1.4.0**、`CHARTER` 1.2.0→**1.3.0**、`DESIGN-SPEC` 1.3.0→**1.4.0**、`M0-RECORD` 1.4.0→**1.5.0**、`M0-PROBE` 1.7.0→**1.8.0**）＋ **3 份记账核对**（`PROJECT-RULES`、`AUDIT`、`AGENT-SYSTEM`）＋ **4 个代理定义 `depends_on` 跟进** | `node tools/sync-check.mjs` 复查 —— 全绿（9 份文档 ＋ 4 个代理定义，0 错误 0 警告） |
| 2026-09-20 | **1.3.0**<br>（**内容第 2 次更新，按 R-D05 属常例**） | **P2** | **拖动诊断就绪 ＋ 死键修复回写，本文件四处改动**：① **§5.4 新增 B-07**（卡片拖动把手实测无效 —— 已定位、未修，含"为什么不用 `resizable: true` 当修复"与修法方向）；② **§6 同步矩阵**更新 `DESIGN-SPEC` → **1.3.0**、`M0-RECORD` → **1.4.0**、`M0-PROBE` → **1.7.0**（三行状态说明重写）；③ **§9 M0 专项**补两条（拖动诊断待跑、`docs/02` 留痕纠错已完成）；④ **页脚版本号更正**（原写 `1.2.0`，与 frontmatter 的 `1.3.0` 不一致）。**代码侧**：新增零依赖 `m0-probe/src/main/diag-flags.js`、离线考裁判 `tools/test-diag-flags.mjs`（`npm test` 四组）、`window.js` 无条件诊断留痕、`card.js` 判别探针、卡片死键修复（`.toast` / `disabled`） | 阿木实测反馈「**不能拖，存在大量无行为（无动作/无进展）的按键**」。核查发现**两条都源自上一轮的失误**：把手"补了但没验、还把结论写成已修"（→ B-07）；底栏按钮"删了一个却留下两个"（与需求 5 移除的「展开全部」同一病灶）。**并且顺线核对时发现 `docs/02` 存在"变更记录说改了、正文没改"的不一致** —— 本项目的铁律是留痕与事实必须一致，故一并纠错 | `docs/02`（1.2.0→**1.3.0**，实质）＋ `docs/05`（1.3.0→**1.4.0**，实质）＋ `m0-probe/README.md`（1.6.0→**1.7.0**，实质）；`agents/design-critic.md` 依赖跟进（记账） | `node tools/sync-check.mjs` 复查 —— 全绿（9 份文档 ＋ 4 个代理定义，0 错误 0 警告）；`node --check` 五个改动文件全部退出码 0；`npm test` 四组全部 PASS |
| 2026-09-20 | **1.3.0** | **P0** | **需求 1 触发 ADR-005 重审 → 本文件第 1 次实质升版，共七处改动**：① **§3.2** WorkerW 边界**翻案**（加删除线保留并注明"原理由未推翻、转为 R12"）；② **§4 M0 节末追加说明** —— 需求 1 与铁律 L5 冲突 → 走 P0 评审 → 结论 **ADR-011**，并**新增追加验收 A10（桌面层挂载可行性，⬜ 未开始）**，同时点明三处口径影响；③ **§5.1** 补第三次更新说明；④ **§5.3 新增「2026-09-20 登记的新需求」表**（用户一次提 5 条，逐条判"范围 / 铁律 / 去向"）；⑤ **§5.4 新增 B-06**；⑥ **§5.5 新增「层级：置顶 vs 桌面层」决策行**；⑦ **§8 新增 R12**、**§9 M0 专项**补三条。另 **§6 同步矩阵全面跟进** | 用户提出 5 条改进需求。**逐条核查代码与文档后的结论**：需求 5 是空壳（**已当场实现 ＋ 三处文档级联**）；需求 2 / 3 / 4 合规，登记进 M1（其中需求 3 是净新增界面结构、需求 4 在设计规范里早有雏形）。**需求 1 与铁律 L5 直接冲突** —— 但核查发现 **ADR-005 早就预登记了重审条件**（原文"若用户强烈反映置顶干扰，重新评估"），**触发条件恰好成立**，故走的是一次**预案内的重审**，而非临时翻案，结论落为 **ADR-011** | 全部下游 —— **3 份实质升版**（`CHARTER` 1.1.1→**1.2.0**、`DESIGN-SPEC` 1.1.2→**1.2.0**、`ARCHITECTURE` 1.2.1→**1.3.0**）＋ **5 份记账核对**（`PROJECT-RULES`、`AUDIT`、`M0-RECORD`、`M0-PROBE`、`AGENT-SYSTEM`）＋ 代码 `m0-probe/src/renderer/card.html`、`card.js` | `node tools/sync-check.mjs` 复查 —— **全绿**（9 份文档 ＋ 4 个代理定义，0 错误 0 警告） |
| 2026-09-20 | **1.2.0**（**记账，不升版**） | P2 | **M0 四项人工判定出结论 → SSOT 全面回填**。① §4 M0 节：标题与结论改为"可判定部分已完成、**收尾卡在验收 6**"，状态行回填 **`6/1/0/1/1`**，验收记录行**关闭"该证据文件当前缺失"**（已重建），剩余动作收敛为"补验收 6 取证"，并**新增一行「验收 6 不通过怎么办」**（先取证再定路径；并指出 §4 与 §8 R9 的降级口径需对齐）。② §5.1 阶段总览、§5.3 任务明细（修正多条**过期状态**：验收 2 原写"待目视"、验收 3 原写"待人工"等）、§5.4 新增 **B-05**（验收 6 不通过）并关闭 B-01。③ §6 同步矩阵：`M0-RECORD` → **1.3.0**、`M0-PROBE` → **1.6.0**。④ §8 `R8` 关闭、`R9` 状态下调并标注与 §4 的口径不一致。⑤ §9 M0 专项清单逐条跟进 | 阿木贴出 `m0-probe/report/M0-验收记录_2026-09-20_03-12-11.md` 并回答四项口径。核查中另发现**两份报告摘要不一致**（自检 `4/0/0/1/4` vs 导出 `5/1/0/2/1`），追查确认差额**全部来自人工标注**；并连带发现两个缺陷：**探针从未实现拖动**、**人工结论不持久化** | SSOT 全文（不升版）＋ `M0-RECORD` 1.2.2→**1.3.0**（实质）＋ `M0-PROBE` 1.5.0→**1.6.0**（实质，新增拖动把手） | `sync-check` 复查全绿（9 份文档 + 4 个代理定义，0 错误 0 警告） |
| 2026-09-18 | 1.0.0 | P0 | 项目初始化；确立宪章、五条铁律、四个里程碑、范围边界 | 用户要求建立可持续维护的项目计划工程书 | 全部 | 创建 6 份文档 + 同步工具 |
| 2026-09-18 | 1.0.0 | — | 历史决策回顾：方向选定 B（晨报机）；送达形态定为常驻桌面卡片；信息源限定为 RSS + API 型；不做导出分享 | 见 `docs/01-立项文档.md` §决策结果 | 产品形态 | 已体现在 §1.1 / §3.1 / §3.2 |
| 2026-09-18 | 1.0.0 | P2 | 同步校验工具首次运行，6 份文档 + 4 个代理定义全部通过；修复工具自身的章节匹配缺陷（精确匹配改为允许编号前缀） | 验证同步机制可用 | `tools/sync-check.mjs` | 报告见 `tools/sync-report.txt` |
| 2026-09-18 | 1.0.0 | P1 | 首轮全面审查完成，登记 40 项缺口（P0×2 / P1×14 / P2×24）。新增 `AUDIT` 文档并纳入同步矩阵 | 用户要求以负责工程师视角审查项目 | `docs/04-项目审查报告.md` | §5.2 与 §6 同步矩阵已更新 |

### ⚠️ 三处留痕纠错（2026-09-22）—— 1.3.0 宣告了、但当时没发生的事

**这是本项目第二次遇到"变更记录宣告了没发生的事"。** 第一次是 `docs/02` 的 `CardFooter`（1.2.0 声称改了 2 处、实际只落 1 处），已记入其 1.3.0。**这次发生在 SSOT 自己身上**：

| 1.3.0 的宣告 | 正文实况 | 本轮处置 |
|---|---|---|
| `§3.2` WorkerW 边界**翻案**（加删除线保留并注明"原理由未推翻、转为 R12"） | ❌ 该行**没有任何翻案痕迹**，与原文一字不差 | 已补：该行现写明"2026-09-20 曾翻案 → 2026-09-22 回归本行"的完整链路 |
| `§5.5` **+层级决策**行 | ❌ 决策表里**根本没有这一行**（只有 A1 / B1 / B12 / B11 / B10 / B13 / R-B12 / A3） | 已补：新增「层级」决策行，含 置顶→桌面层→置底 的完整决策链 |
| `§8` **+R12** | ❌ 风险登记表**只到 R11** —— 而 `§4`、`§9`、`docs/04`、`项目规则.md` **四处都在引用"§8 R12"** | 已补：R12 登记为"已规避"，并把这次登记瑕疵写进它的状态列 |

**为什么它会漏**：三处都是"**在别的段落里被引用、却在正文里没落笔**"的类型 —— 分别是一条**表格行**、一行**决策行**、一行**风险行**，都在同一次大批量编辑里被跳过。
**机器为什么没抓住**：`sync-check` 查的是**版本号与核对留痕**，**它不校验"变更记录描述的动作是否真的发生在正文里"**。
**⇒ 这暴露了一条真实的检查缺口**：改动"一条表格行"这类动作最容易在大批量编辑里静默丢失，而**没有任何机器检查看着它**。已登记为待办（见 `§9` 通用清单）。
**⇒ 与 `docs/02` 那次的共同点**：两次都是**批量编辑 + 变更记录先行**。**降险做法：变更记录写完必须回读正文逐条核对，而不是凭记忆写"影响了哪几节"。**
| 2026-09-18 | — | — | **本次审查新增两项 P0 待办**：A1（R6 API Key 方案必须决策）、B1（透明窗口 + 混合 DPI 缺陷，并入 M0） | 见 `docs/04-项目审查报告.md` §2 | M0 验收标准 / M2 前置 | 待处理，见审查报告 §5 处理顺序 |
| 2026-09-18 | — | P2 | `AUDIT` 1.0.0 → 1.0.1：修正 B1 判定错误（原"高 DPI 尺寸变大"判断不成立），重写为"透明无边框窗口 + 多显示器混合缩放的 DPI 缺陷"，M0 验收标准由 1 条扩为 4 条 | 自查发现原技术判断有误，真实风险更严重 | `docs/04-项目审查报告.md` | §6 同步矩阵版本号已同步 |
| 2026-09-18 | **1.1.0** | **P1** | **两个 P0 决策同时落地，本文件六处改动**：① §2.2 中期目标第 3 条（API Key 门槛）标记为已决策；② §3.2 新增"不做模型调用中转服务"；③ §3.3 待定区填入两行（泛大众化 + 已读未读状态），泛大众化附两个重新评估触发条件；④ §4 M0 验收标准 **5 条 → 9 条**（新增三档缩放一致性、双屏混合缩放、跨屏坐标偏差、`scaleFactor` 可靠读取）；⑤ §4 M2 验收标准放宽为"五分钟 **+ 一次 API Key 配置**"；⑥ §5 新增 §5.5 待决策与已决策表、§5.4 新增阻塞项 B-02 | **用户要求"先把两个 P0 一起摆到桌上谈一轮"**，并已作出决策：A1 → 纯本地·自带 Key；B1 → 完整覆盖 + 可切换降级 | 全部下游 | 见下方级联明细 |
| 2026-09-18 | **1.2.0** | **P1** | **M0 实施完毕，本文件八处改动**：① §4 M0 状态改为「自动可判部分完成」，补交付物、验收记录指针与剩余动作；② §5.1 阶段总览 M0 标为 ✅ 85%，并说明"85% 怎么算的"；③ 新增 **§5.2b M0 交付物表**；④ §5.3 任务明细拆为「M0 已收尾」+「M1 前置」两张表，逐条填真实状态；⑤ §5.4 新增 B-03/B-04，B-01 降级为"只差人眼"、B-02 大幅降级（实测证明与多屏无关）；⑥ §5.5 新增三条已决策（B10/B11/B12）；⑦ §6 同步矩阵纳入新文档 `M0-RECORD`，标注 `AGENT-SYSTEM` 待跟进 + 说明 CHARTER/DESIGN-SPEC 为何不升版；⑧ §10 项目地图补 `docs/05` 与 `m0-probe/` | **用户指示"现在开工"**，M0 探针跑通并产出真实结论；同时抓出 5 个静默失败级问题（B8–B12），其中 3 项已固化为代码层强制约定 | 全部下游 | 见下方级联明细 |
| 2026-09-18 | **1.2.0**<br>（**内容更新，刻意未升版**） | **P2** | **M0 验收记录回填，本文件五处改动**：① §4 M0 状态由「通过 2 / 有保留 1 / 未判定 5」改为「**通过 4 / 有保留 0 / 未判定 4**」，剩余动作由"人工判定 6 项"改为 4 项，并追加进度更新块；② §5.1 阶段总览 M0 改为「5 条结论明确 + 4 条待人工」、88%；③ §6 同步矩阵更新 `M0-RECORD` 1.0.0→**1.1.0**、`M0-PROBE` 1.1.0→**1.2.0**；④ §9 M0 专项清单跟进；⑤ 本行。**代码侧**（见 `docs/05` §6.5）：验收 3 接线 + 验收 2 截图法落地 | 用户授权「按第 1、2 步动手」——把可量化的判据交给工具，取消人工核验环节 | `docs/05-M0验收记录.md`、`m0-probe/README.md` | **刻意不升版本号的完整理由见下方「一次被刻意跳过的升版」** |
| 2026-09-19 | **1.2.0**<br>（**内容第 2 次更新，仍未升版**） | **P2** | **三项处置的结论回写，本文件四处改动**：① **§6 同步矩阵全面补齐（两轮积压）** —— 原表里 `AUDIT` 行还停在 1.2.0、`M0-RECORD` 停在 1.1.0，即**连续两轮都没跟上**；现更新为 `PROJECT-RULES` **1.3.0**、`DESIGN-SPEC` **1.1.2**、`ARCHITECTURE` **1.2.1**、`AUDIT` **1.4.0**、`M0-RECORD` **1.2.1**、`M0-PROBE` **1.4.0**、`AGENT-SYSTEM` **1.1.2**，代理定义 5 条全部更新，并把 `DESIGN-SPEC` 的状态由「仅依赖跟进」改判为**实质变更**；② §6 更新 `AGENT-SYSTEM` 的"待跟进"说明（原来只提 B10–B12，现补 §5.8 / R-B11），并如实记下**这一轮 `design-critic` 确实被实质改了**（1.1.1 → 1.2.0）；③ §7 新增本轮级联明细；④ 本行。**未升版的理由见下方「第二次刻意不升版」** | 用户拍板「**1.改 2.改为「落盘错误上下文 + 明确的退出策略」，与 B3 合并处理 3.跑 npm run calibrate**」。三项处置全部落地（S-01 措辞级联、B13 闭环、G4 闭环 ＋ 新规则 R-B10 / R-B11 ＋ 新技术债 TD-06），**SSOT 的状态描述必须与下游对齐**，否则同步矩阵形同虚设 | 全部下游 —— 本次已**逐份核对**，共 9 个文件，明细见下方 | 见「本轮级联明细（2026-09-19）」 |
| 2026-09-19 | **1.2.0**<br>（**内容第 3 次更新，仍未升版**） | **P2** | **C+E 机制上线 ＋ 全库核对留痕补齐，本文件四处改动**：① **§5.5 机制决策落定** —— 「记账式改动是否升版」由 🅿️ *需用户拍板* 改为 ✅ **已决策 = C+E**；② **§5.3** 把「`sync-check.mjs` 增强」标为 ✅ **已落地，但换了方案**（原方案是逐载体比版本号 → 改为**改查核对留痕**），并新增一行登记校验器的**两项新检查**（核对留痕 / 表格完整性）；③ **§6 同步矩阵**更新 `PROJECT-RULES` → **1.4.0** 及三处下游的 `depends_on`，并补写「上游需同步＝是」**这一列的含义变化**（从"要升版"改为"要核对 + 留痕"）；④ 本行。**未升版的理由变了 —— 见下方「C+E 已上线」** | 用户拍板「**按照你推荐的方案进行**」，即采纳 **C+E 合并形态**。落地载体：`项目规则.md` **1.4.0**（改写 R-D03 ＋ 新增 R-D05）＋ `tools/sync-check.mjs` 新增第 6 / 7 项检查。**这是本文件第 3 次"内容更新不升版"，也是最后一次以"例外"身份出现** —— 此后它由 **R-D05** 直接授权，是常例，不再需要每次专门登记 | 全部下游 —— 本轮**逐份核对共 9 个文件**（`CHARTER`、`DESIGN-SPEC`、`ARCHITECTURE`、`AUDIT`、`M0-RECORD`、`M0-PROBE`、`AGENT-SYSTEM`、`curator`、`design-critic`），**全部记账、0 个升版** | 见「C+E 已上线（2026-09-19）」 |
| 2026-09-20 | **1.2.0**<br>（**内容第 4 次更新，按 R-D05 属常例**） | **P2** | **M0 验收 3 的校准结论回写 ＋ 同步矩阵跟进，本文件五处改动**：① **§4 M0 节** —— 验收 3 的裁判校准状态由「未执行」改为 ✅ **已完成**（2026-09-19 **11:20**，`CALIBRATED · 裁判有效`），并把"通过"的含义从"判据成立、观察到 0 次抢焦点"更正为"**判据成立，且该裁判已被证明能识别出坏情况**"；同时补记校准器的**留痕污染缺陷**及其修复；② **§4 M0 表格**的"验收记录"一行标注 `_selftest-report.md` **当前缺失**及原因；③ **§5.1 阶段状态总览**同步该结论；④ **§6 同步矩阵**更新 `M0-RECORD` 1.2.1 → **1.2.2**、`M0-PROBE` 1.4.0 → **1.5.0**（含两行状态说明）；⑤ **§9 M0 专项清单**把"裁判校准未执行"改为打勾，并新增一条遗留项「重跑一次干净的 `npm run selftest`」；⑥ 本行 | 用户「已在后台完成前序任务」。核查确认**验收 3 的校准已成功**，同时发现两件必须回写的事：① 下游 `M0-RECORD` 的结论已**实质变化** —— **其内已不存在"通过但裁判未校准"的项**；② 验收的**证据文件被校准器那次内部自检覆盖**（`M0-RECORD` 已归档 + 校准器已加隔离与守卫修复）。**SSOT 的状态描述必须与下游对齐，否则同步矩阵形同虚设** | 本轮实际升版的只有 `M0-RECORD`（→ 1.2.2）与 `M0-PROBE`（→ 1.5.0）两份；其余下游经核对**无需改动**（记账，0 升版） | `node tools/sync-check.mjs` → 9 份文档 + 4 个代理定义，**0 错误 0 警告、退出码 0** |

> ### ⚠️ 第二次刻意不升版 —— 并且这件事现在必须处理了（2026-09-19）
>
> > ✅ **本节的处置已于同日结案：采纳 C+E 合并形态。** 下方原文**一字未删**（§7 只增不删 —— 它记录了这个问题**是怎么被发现的**，
> > 而"发现问题"和"解决它"是两件同样值得存档的事）。**落地方案与实测对比见紧随其后的「C+E 已上线」。**
> > 结论一句话：这次的不升版**不再是例外，而是规则本身**（`R-D05`）。
>
> **先说结论**：本文件这次**又没有升版**，仍保持 **1.2.0**。理由与上次相同（记账式升版会稀释版本号、代价会反向惩罚 SSOT）。
>
> **但性质变了。** 上次的原话是：
>
> > 「在它有结论之前，本文这次的'未升版'是一个**有记录的例外**，不是一个已经建立的惯例。」
>
> **现在它发生了第二次。** 而本项目自己最警惕的正是这类东西：
> **一条规则一旦被重复执行两次，它事实上就已经存在了 —— 无论有没有人决定过它。**
> 这意味着**"记账式改动不升版"正在未经决策地变成惯例**，而这恰恰是上次那条原话想避免的结果。
>
> **不掩盖，也不仓促定案。处置如下：**
>
> 1. **把这件事从「M1 前置任务（与其它项并列）」升级为「需要用户拍板的决策项」** —— 它是**唯一**一个越拖越贵的机制问题（每次内容更新都在多欠一笔账）。
> 2. **我的推荐：方案 C 与 E 的合并形态。**
>    - **方案 C**：接受成本，但把"纯依赖跟进"从 patch 升版改为**不改版本号、只在变更记录留痕**（把上次的例外制度化）；
>    - **方案 E（本次新增建议）**：把两件事**解耦** —— **版本号只表示"这份文档自己变了"**，核对义务改用"**留痕**"表达；
>      于是 `sync-check` 的检查项从「下游声明的版本 == 上游当前版本」改为「**下游是否有针对上游当前版本的核对记录**」。
>      这样**机器仍然守得住**（留痕缺失照样报 FAIL），但**不再逼着 12 份文档为一次记账各 +0.0.1**。
>    - 我推荐 **C+E**，理由：它保住了"机器可校验"这一条底线（方案 A/B 会把判断逻辑变复杂，而 C 单独用会削弱校验器），
>      代价是 `sync-check` 需要一次小改（从"比版本号"改为"找留痕"）。**方案 A / B 我暂不推荐** —— 它们要求每份文档声明"自己读了上游哪一部分"，
>      而实践证据（本次 1 真消费者 vs 7 记账）说明**声明粒度本身不会被认真维护**。
> 3. **在拍板之前：继续沿用"有记录的例外"，但每次都必须显式登记**（本次即本行）。
>
> **⚠️ 这次还暴露了第二个机器不查的版本载体**（见 `docs/05` §7.3）：
> **代理定义的文件末尾页脚**。四个代理的 frontmatter 已升版、**页脚却停在上一版**，
> 而 `sync-check` **只读 frontmatter、不读页脚** —— 所以这个错误**永远不会让校验失败**。
> 已修，并在 `agents/README.md` §6.3 立了规则：「版本号有三个载体（frontmatter / 页脚 / `registry.json`），必须同一次改完」。
> **它同时是 `MASTER-PLAN` 候选规则「同一份受管文件必须整块一致修改」的第一个真实案例** —— 那条规则原本只是"待走流程"，现在有了实证。

### ✅ C+E 已上线（2026-09-19）—— 不升版从此是**规则**，不是例外

**用户拍板原话**：「**按照你推荐的方案进行**」→ 采纳 **C+E 合并形态**。

#### 一、机制本身（三句话说完）

| # | 改了什么 | 一句话 |
|---|---|---|
| 1 | **`depends_on` 语义精确化** | 从「**我依赖** `ID` 的 `V` 版」→ 「**我已核对** `ID` 到 `V` 版」。**这一个字的差别是全部设计的支点** —— 它把一句模糊的依赖声明，变成了一个**可执行的动作承诺** |
| 2 | **检查从"两道闸"变"三道闸"** | ① 依赖必须存在；② 声明的版本必须**等于**上游当前版本（否则报 `[未跟进]`）；③ 必须留下**含确切版本号**的核对痕迹（否则报 `[缺留痕]`）。**③ 是新增的那一道** |
| 3 | **记账 vs 实质（`R-D05`）** | **记账**（**不改版本号**）：更新 `depends_on`、补写核对留痕、回填同步矩阵 / 进度数字 / 正文引用的版本号。**实质**（**必须升版**）：规则要求变了、新增/删除规则、结论 / 契约 / 职责边界变了、代码行为或设计口径变了 |

**留痕位置的硬性规定** —— 这是"机器可校验"能成立的前提：

- **文档** → 自己的 `## 变更记录` 章节里，必须出现字面量 `doc_id@版本`
- **代理定义** → 自己的 `## Lessons`，**或** `agents/README.md` 的「代理系统变更记录」（两者认其一，因为代理的核对记录按既有约定集中记在后者）
- **⛔ frontmatter 不算，页脚也不算。** 原因很硬：`depends_on: X@1.0.0` 那行**自身就含** `X@1.0.0`，页脚也写着 `上游：X@1.0.0` —— 把任一处算数，都等于**让检查自己满足自己**。

#### 二、为什么级联就停在一轮了

推导只有一步，但它值一整轮工作量：

```
上游升版 → 下游核对（改 depends_on + 写留痕，不升版）
        → 下游的版本号没变 → 它的下游不会过期 → 终止 ✅
```

**旧机制**里，下游"**仅仅为了跟上**"也要升 patch → 下游的下游跟着过期 → **递归**。
（实证：上一轮"改一句 S-01 措辞"带出 9 个文件并继续向第二轮扩散。）

#### 三、实测对比（同一天、同一批改动下的两次推演）

| 环节 | **旧机制**（仅跟进也升 patch） | **新机制**（记账不升版） |
|---|---|---|
| 触发 | `PROJECT-RULES` 1.3.0 → **1.4.0**（实质变更） | 同一个 `PROJECT-RULES@1.4.0` |
| 第一轮 | 5 个下游各升一个 patch | 5 个下游**全部记账，0 个升版** |
| 第二轮 | 那 5 个的**新版本号**又让 `M0-PROBE` 等过期 → 再来一遍 | **不存在**（下游版本号未变，无从触发） |
| 净效果 | 改动量 = 两轮累加；且**每次都要重新核对一遍刚核对过的东西** | **一轮到底** |
| 依据 | ⚠️ 按拓扑序**推演**（**未实跑**，如实标注） | ✅ **实跑** —— `sync-check` 当场报出 5 项 `[未跟进]`，预测完全命中 |

**本轮 5 个下游（全部记账、0 升版）**：`AGENT-SYSTEM`、`AUDIT`、`M0-RECORD`、`curator`、`design-critic`。

#### 四、上线时**新检查自己抓出**的两个真缺陷

> 强调"自己抓出"是因为：**这两类问题此前没有任何检查能发现，肉眼也看不出。**

1. **⭐ 我自己代码里有个真洞 —— 页脚替下游把留痕"写"掉了。**
   `extractSection('变更记录')` 从该节一直取到**文件末尾**，而**页脚就在末尾**；页脚写着 `上游：MASTER-PLAN@1.2.0`，
   于是**检查自己满足了自己**。我在注释里明明写着"不认页脚"，**代码却没做到**。
   - **发现方式**：`docs/01` 的变更记录里其实**没有** `MASTER-PLAN@1.2.0`，它却没报 FAIL —— 与我的推断矛盾，于是回查代码。
   - **修法**：在 `extractSection` 里加页脚行过滤（只去 `*…*` 形式的页脚行，`**加粗**` 不受影响）。
   - **后果**：真实缺留痕从 **2 项跳到 8 项** —— **此前有 5 个文件是靠页脚蒙混过关的。这个洞不修，整套新机制就是假的。**
2. **⚠️ 表格被空行断成两截（全库 7 处）** —— `docs/01` L322、`docs/02` L754、`docs/04` L295、`docs/05` L521 与 L523、`项目规则.md` L462、本文件 L412。
   Markdown 渲染出来只是"表格断了一截"，**看起来像是作者有意为之**，所以肉眼审阅抓不到。
   其中 `docs/05` 与 `项目规则.md` 那两处，是**上一轮编辑时我自己埋下的**。
   - **修法**：加**第 7 项检查**（表格完整性）当 oracle 全部找出来 —— 判据是"某行以 `|` 开头、上面隔着空行最近的非空行也是 `|` 开头"；
     并含一条**误报排除**：若该行**后跟分隔行**（`|---|`），说明那是新表的表头，放过。

> **📌 一条当场发生的注脚，比上面两条都更能说明问题**：
> 写这一节的过程中，我往本文件 §7 变更记录表追加新行时，**又多留了一个空行 —— 表又断了**（第 420 行，就在这份表格里）。
> 而**第 7 项检查在下一秒就报了 WARN，位置精确到行号**。
>
> 这条注脚的价值在于：**它抓的不只是"历史欠账"，也包括"刚刚写下的东西"。**
> 一个只在首次运行时有效的检查器，和一个**每次运行都有效**的检查器，是完全不同的两种东西 ——
> 前者是打扫卫生，后者是**地板本身**。
>
> 顺带说，那个"漏一个空行"的手滑**今天已经犯了第三次**（前两次在 `docs/05` 与 `项目规则.md`，都是上一轮埋的）。
> **人在重复动作上一定会出错 —— 所以这类错误不该由人的注意力来兜底。**

#### 五、校验结果

`node tools/sync-check.mjs` → **全部通过**：**9 份文档 + 4 个代理定义**，**0 错误、0 警告**，退出码 **0**。
检查项本次由 **5 项扩到 8 项**（详见 §5.3）；其中第 8 项（§6 版本表 vs frontmatter）已做**变异测试**验收 ——
故意改错一处版本号，它当场报出预期 WARN，**位置与措辞均正确**。

#### 六、顺带记一笔：**第 4 个"机器不读的版本载体"**

此前已记过三个：① §7 变更记录里**声明的目标版本**；② §6 同步矩阵里**写的版本号**；③ **代理定义的末尾页脚**。
三者的共同点是"**人写、机器不读，于是必然漂移**"。本轮又添一个 ——

> **根 `README.md`。** 它第 6 行列着「项目文档版本：`PROJECT-RULES` 1.2.0 …」，而 `sync-check` **有意跳过根目录的 README**
> （它是给人看的门面，不是受管文档）。于是它也漂了，而且漂得比其它载体更早 —— 落后**两个 minor**；
> 连"45 项缺口 / 已闭环 8""37 条规则""5 个静默失败级问题""验收 4/0/0/1/4"这些**事实性数字**也全部过期。

**处置：不修副本，直接删掉那串版本号，改为指向 §6 同步矩阵。**
理由与 C+E 同源 —— **"逐个追副本"是打地鼠（每加一个载体就多一个补丁），"去掉副本"才是根治。**

---

### 本轮级联明细（2026-09-19）—— MASTER-PLAN 保持 1.2.0

**起点**：用户拍板三项处置。**其中两件在处置过程中又各自制造了新的升版源**（`PROJECT-RULES` 1.3.0、`ARCHITECTURE` 1.2.1），于是级联比预估的宽。

| 文件 | 版本变化 | 类型 | 核心改动 |
|---|---|---|---|
| `docs/02`（DESIGN-SPEC） | 1.1.1 → **1.1.2** | **实质** | §3.7 与 S-01 措辞更正（3 处）；明确「禁令针对**值**，不是属性名」 |
| `项目规则.md`（PROJECT-RULES） | 1.2.0 → **1.3.0** | **实质** | +**R-B10**（主进程失败语义：同步落盘 + 明确退出策略）、+**R-B11**（不许被 `electron` 的 import 绑架）；速查表 37 → 39 |
| `docs/03`（ARCHITECTURE） | 1.2.0 → **1.2.1** | **实质** | +§5.8（拆零依赖模块）、+**TD-06**；静默失败族 5 项 → **6 项（B8–B13）** |
| `docs/04`（AUDIT） | 1.3.0 → **1.4.0** | **实质** | B13 **闭环**；+**G4**（判定逻辑被 electron 绑架）并闭环；登记 R-B10/R-B11/TD-06；进度 `47/10/37` |
| `agents/design-critic.md` | 1.1.1 → **1.2.0** | **实质** | **第 18 项检查改写** —— 原文只禁属性名，照字面会把 `card.css` 5 处合规写法判成 block（**假阳性**） |
| `agents/brief-writer.md` | 1.1.1 → **1.1.2** | 仅跟进 | 无需改动（S-01 针对组件 CSS，简报生成不写 CSS） |
| `agents/collector.md` | 1.0.2 → **1.0.3** | 仅跟进 | 无需改动（§5.8 / TD-06 都在主进程侧）。**依赖铁律 D3 第三次兑现** |
| `agents/curator.md` | 1.1.1 → **1.1.2** | 仅跟进 | 无需改动。执行清单第 10 步**第二次派上用场**（1 实质 + 7 跟进，没一起升 minor） |
| `agents/README.md`（AGENT-SYSTEM） | 1.1.1 → **1.1.2** | 仅跟进+补充 | §6.3 补「版本号有三个载体」；§7 变更记录 8 行；§9 清单版本更新 |
| `agents/registry.json` | — | 同步 | 4 个代理版本 + `dependsOn` + `versionChangeTypes` + `pageFooterErratum` + 新 `changeLog` 条目 |
| `docs/05`（M0-RECORD） | 1.2.0 → **1.2.1** | 仅跟进+结账 | §7 由「有意搁置」转为**结账**；+§7.3（页脚漂移）；§3 的 B13 行改 ✅ |
| `m0-probe/README.md`（M0-PROBE） | 1.3.0 → **1.4.0** | **实质** | B13 处置明细 + 四个必须写下来的决定；+两个离线考裁判工具与 `npm test`；目录标注零依赖/待拆 |
| `项目计划工程书.md`（MASTER-PLAN） | **保持 1.2.0** | 内容更新 | 见上方「第二次刻意不升版」 |

> **⚠️ 一个值得记住的偏差**：预估是 **7 个文件**，实际是 **9 个**。
> 多出来的三个（`AGENT-SYSTEM`、`curator`、`registry.json`）**不在用户点名的那份文档的闭包里** ——
> 它们是"**为了修它而顺带升版的另外两份文档**"带出来的。
> **教训：估算级联规模时，不能只追踪"用户点名的那一份"。修得越彻底，级联越宽。**

---

> ### ⚠️ 一次被刻意跳过的升版（2026-09-18，请勿当作遗漏）
>
> **发生了什么**：本次按 `docs/05` §6.5 落地了验收 2、3 的自动化。连带地，本文件 §4/§5/§6/§9 里记录的 M0 状态数字需要与 `docs/05` 对齐 —— 于是本文件内容被更新了。
>
> **原本打算怎么做**：按惯例把 `MASTER-PLAN` 升到 **1.2.1**。
>
> **升版后会怎样**（已实测，不是推测）：`node tools/sync-check.mjs` 会立刻报 **7 项 FAIL** ——
> 凡 `depends_on` 里写着 `MASTER-PLAN@1.2.0` 的下游（`PROJECT-RULES` / `CHARTER` / `DESIGN-SPEC` / `ARCHITECTURE` / `AUDIT` / `AGENT-SYSTEM` / `curator`）全部过期。
> 这 7 份升版之后，又会引发**第二轮**过期：`AUDIT` 依赖 `CHARTER@1.1.1`+`DESIGN-SPEC@1.1.1`+`PROJECT-RULES@1.2.0`，
> `M0-RECORD` 依赖其中 4 份，`M0-PROBE` 依赖 `ARCHITECTURE`+`M0-RECORD`，另有 4 个代理定义也各自挂靠。
> 按拓扑序老实跟完 = **12 个文件、约 48 处纯记账式编辑**，其中没有一处改变任何实际内容。
>
> **为什么最终不这么做** —— 三条理由，按重要性排序：
>
> 1. **依据不在我这边。** §0.1 铁律 2 的原话是「每个阶段完成必须**更新**三处：§5 进度看板、§6 同步矩阵、§7 变更记录」，用的是"**更新**"；
>    而 §0.2「变更分级」管的是"**变更**"。本次做的是**状态回填**（把本文件里记的 M0 状态，与已经写在 `docs/05` 的结论对齐），不是变更。
>    **更新内容 ≠ 升版本号。**
> 2. **记账式升版会稀释版本号的信息量。** 版本号的意义是"这份文档的实质内容变了"。
>    若每改一个数字就让 12 份文档各 +0.0.1，很快就没有人能从版本号里读出任何东西 —— 而这恰恰是这套体系最想保住的东西。
> 3. **代价会反向惩罚 SSOT。** 如果"改一句 SSOT 的状态描述"要付 48 处编辑的成本，理性的做法就变成"别改 SSOT"。
>    那正好废掉 §0.1 铁律 1（变更先改这里）。**机制的成本不能高到让人绕开机制。**
>
> **⚠️ 但这暴露了一个真实的机制缺陷，必须登记，不能被这次的"跳过"掩盖**：
>
> > **当前 `depends_on` 只有"文档级"依赖，没有"接口级"依赖，于是任何上游改动都会波及全部下游，无论是否相关。**
> >
> > 本次就是活例子：M0 的状态数字变了，`CHARTER`（产品定位）与 `DESIGN-SPEC`（视觉规范）**在语义上完全不受影响**，
> > 却因为"依赖 MASTER-PLAN"这个粗粒度声明而一并过期。
> >
> > **候选解法**（待决策，未实施）：
> > - 方案 A：`depends_on` 支持**范围标注**，如 `MASTER-PLAN@1.2.x#scope=goals,scope=milestones`，只有被依赖的范围变化才触发下游核对；
> > - 方案 B：给文档加 `version_scope` 概念 —— SSOT 声明"本次变化影响哪些章节"，`sync-check` 按章节交集判断下游是否需要跟进；
> > - 方案 C：接受成本，但把"纯依赖跟进"从 patch 升版改为**不改版本号、只在变更记录留痕**（即把本次的例外**制度化**）。
> >
> > **已登记为 M1 前置任务**（与 `design-critic` 第六组、`sync-check` 增强并列）。
> > 在它有结论之前，本文这次的"未升版"是一个**有记录的例外**，不是一个已经建立的惯例。
>
> **本次的实际影响范围**（已核对，确认无遗漏）：`MASTER-PLAN` 内容更新、版本号保持 1.2.0；
> `M0-RECORD` 升 1.1.0、`M0-PROBE` 升 1.2.0（这两份是**实质变更**，不是依赖跟进）；其余 9 份文档与 4 个代理定义**一律不动**。

> **下方明细小节的顺序：新的在前。** （`MASTER-PLAN 1.2.0 的内更新` 在上，`1.1.0 → 1.2.0` 居中，`1.0.0 → 1.1.0` 在下。）

### 本次变更的同步明细（MASTER-PLAN 保持 1.2.0）

| 下游 | 版本变化 | 核心改动 |
|---|---|---|
| **`M0-RECORD`** | 1.0.1 → **1.1.0**（**实质变更**） | §0 数字回填 `4/0/0/1/4` ＋ 新增 §0.1 变化对照表；§1 验收 2、3 重写（含验收 2 的实测原始读数、验收 3 修掉的两个问题）；§5 收尾清单跟进；§6 / §6.2 / §6.5 标注落地进度 |
| **`M0-PROBE`** | 1.1.1 → **1.2.0**（**实质变更**） | 验收表 2/3 改为自动判定并更新结果；新增"更动预告"进度与三条必知事项；变更记录补 `blurmeasure.js` / 焦点测试三态 / `runFocusTestAndRecord()` 等全部代码改动 |
| `MASTER-PLAN` | **保持 1.2.0**（内容更新，**未升版**） | 见上方「一次被刻意跳过的升版」 |

**本次无其他下游改动**：验收 2/3 转自动判定**不改变产品定位、不改变视觉规范、不改变架构分层** ——
判定代码住在 `m0-probe` 探针内部，不进产品代码。故 `CHARTER` / `DESIGN-SPEC` / `ARCHITECTURE` / `PROJECT-RULES` / `AUDIT` / `AGENT-SYSTEM` 与 4 个代理定义**均不触发**。
**已用 `node tools/sync-check.mjs` 复核：全绿。**


### 本次 P1 变更的同步明细（1.1.0 → 1.2.0）

| 下游 | 版本变化 | 核心改动 |
|---|---|---|
| `PROJECT-RULES` | 1.1.0 → **1.2.0**（实质） | 新增 **R-B07**（ESM 主进程禁止顶层 `await app.whenReady()`）、**R-B08**（窗口尺寸与坐标硬规则：构造参数不可信 + 坐标物理像素网格对齐）、**R-B09**（Electron 必须经唯一启动入口启动）；速查表（34 → 37 条）与变更记录同步 |
| `ARCHITECTURE` | 1.1.0 → **1.2.0** | 新增 **ADR-009**（窗口尺寸与坐标的物理像素不变量）；目录补 `tools/launch-electron.mjs`、`tools/win-display-facts.ps1`、`experiments/`；开机心跳纳入可观测性；M0 实测结论写入 §5.6 |
| `AUDIT` | 1.1.0 → **1.2.0** | 新建 **§6「M0 实施新发现」**（B8–B12 五项，各含症状/根因/取证/处置/诚实说明）；§1 总览表新增 3 行并调整 2 行状态；§0 统计口径改为以总览表逐行数为准（修正 P1 计数不符）；§5 第二批标注 B2/D1 已在探针落地 |
| **`M0-RECORD`** | **新建 1.0.0** | M0 验收记录：九条逐条留痕 + 环境基线 + 5 个新发现索引 + 对 M1 的资产清单 + 收尾检查清单 |
| `CHARTER` | 1.1.0 → **1.1.1**（仅依赖跟进） | 已核对：M0 结论不改变产品定位与目标用户，**正文未改**，仅 `depends_on` 跟进到 `MASTER-PLAN@1.2.0`；标题与页脚版本号同步 |
| `DESIGN-SPEC` | 1.1.0 → **1.1.1**（依赖跟进 + 尾部补记） | 已核对：M0 结论**不改变任何视觉规范与双形态令牌**（未触发降级，双屏无法验证），规范正文一字未改；仅在末尾追加一节实测说明，并跟进依赖到 `MASTER-PLAN@1.2.0`。B12 属实现层规则，已写入 ADR-009 |
| `AGENT-SYSTEM` | 1.1.0 → **1.1.1**（仅依赖跟进）＋ ⏳ 一个真缺口 | 依赖跟进到 `MASTER-PLAN@1.2.0, PROJECT-RULES@1.2.0`；§7 追加 6 行变更记录、§9 代理清单版本更新、`registry.json` 全量核对。**⚠️ 另有一个真缺口**：`design-critic` 该有一组「平台硬规则」检查项（覆盖 B10/B11/B12），属**代理定义实质变更**，须走 curator 流程，**已登记为 M1 前置任务**，不在本次硬塞 |

**代理定义级联（5 个全部"仅依赖跟进"，内容一字未改）**：`collector` 1.0.1→**1.0.2** ｜ `brief-writer` 1.1.0→**1.1.1** ｜ `design-critic` 1.1.0→**1.1.1** ｜ `curator` 1.1.0→**1.1.1** ｜ `arch-critic` dependsOn 跟进（未创建）。

> **⚠️ 但核对时发现了一个真缺口**：`design-critic` 该有一组「平台硬规则」检查项（覆盖 B10/B11/B12），
> 而它现在没有 —— 这三个静默失败问题**正是审查该拦住却没有检查项的那一类**。
> 这是**代理定义的实质变更**，须走 curator 流程，**已登记为 M1 前置任务**（见 `agents/README.md` §7 与 `registry.json` 的 `pending` 字段），
> 本轮**刻意未执行** —— 不为凑同步而顺手改。

> **一条自我提醒（已兑现，而且很打脸）**：v1.1.0 时出现过"改完六份文档跑校验报 1 个 FAIL（漏了某个 `depends_on` 版本号）"。
> 本版的做法是：**先改全部文档，最后统一跑 `node tools/sync-check.mjs`**，并**逐个核对 `depends_on` 里的每一个版本号**，
> 包括"我没改这份文档、但它的上游升版了"这一类。
>
> **结果：第一次跑 8 项 FAIL，修完才全绿。** 8 项里**没有一项是"文档内容写错"**，全是**"我以为我改过、其实没落盘"**。
> 详见下方「1.2.0 同步校验结论」——那里也记了这类错误要怎么变成规则和工具检查项。
>
> **范围声明**：本节末尾的这些补齐动作（frontmatter 版本号、校验结论）都在 **1.2.0 这一次变更的范围之内** ——
> 属于"补齐已声明但未落地的部分"与"兑现承诺的校验结论"，**不产生新版本，也不触发新一轮级联**。


### 1.2.0 同步校验结论

**命令**：`node tools/sync-check.mjs` ｜ **日期**：2026-09-18 ｜ **退出码**：`0` ｜ **结果**：**全部通过**
**扫描范围**：9 份文档 + 4 个已定义代理（`arch-critic` 为 DRAFT，尚无定义文件，不计入）
**原始输出留档**：`tools/sync-run.txt`（本节表格即由它抄录，可复跑核对）

| 文档 | 版本 | 代理定义 | 版本 |
|---|---|---|---|
| `MASTER-PLAN` | 1.2.0 | `collector` | 1.0.2 |
| `PROJECT-RULES` | 1.2.0 | `brief-writer` | 1.1.1 |
| `ARCHITECTURE` | 1.2.0 | `design-critic` | 1.1.1 |
| `AUDIT` | 1.2.0 | `curator` | 1.1.1 |
| `M0-RECORD` | 1.0.0 | —（`arch-critic` DRAFT，未建） | — |
| `CHARTER` | 1.1.1 | | |
| `DESIGN-SPEC` | 1.1.1 | | |
| `AGENT-SYSTEM` | 1.1.1 | | |
| `M0-PROBE` | 1.1.0 | | |

#### 第一次跑出来的 8 项 FAIL（诚实记录）

不是文档写错，是**我上一轮以为落盘了的编辑有一半根本没写进去**。

| # | FAIL 内容 | 真因 | 处置 |
|---|---|---|---|
| 1-2 | `AGENT-SYSTEM` 声明依赖 `MASTER-PLAN@1.1.0` / `PROJECT-RULES@1.1.0`，实际为 1.2.0 | `agents/README.md` 的 **frontmatter 未落地** —— 正文 §7 变更记录、§9 清单、页脚**都改了**，头部 `version` 与 `depends_on` 仍是旧值 | 补 frontmatter：`version: 1.1.1`、依赖升 1.2.0 |
| 3 | `M0-RECORD` 声明 `DESIGN-SPEC@1.1.0`，实际为 1.1.1 | **新建时就写错了**：抄了旧版号，没有回头核对上游 | 改为 1.1.1 |
| 4 | `curator` 声明 `AGENT-SYSTEM@1.1.1`，实际为 1.1.0 | **下游先改、上游没改** → 出现"下游期望上游比自己新"的循环不一致（下游没错，是上游漏了） | 上游 `AGENT-SYSTEM` 升到 1.1.1 后自然一致 |
| 5-8 | `registry.json` 记 `collector 1.0.2` / `brief-writer 1.1.1` / `design-critic 1.1.1` / `curator 1.1.1`，四个定义文件仍是旧版本 | **四个代理定义文件的 `version` 字段未落地**（`depends_on` 落了、`version` 没落 —— 同一文件里改了一半） | 四个文件 `version` 逐一补齐 |

#### 这 8 项 FAIL 的共性（值得记下来）

**全部是"同一份文件里改了 A 处、漏了 B 处"**：

- 改了**正文**，漏了 **frontmatter**（`agents/README.md`）
- 改了 **`depends_on`**，漏了 **`version`**（4 个代理定义）
- 新建文件时**抄了旧版号**，没有回头核对（`docs/05`）

这类错误**靠肉眼核对几乎必漏** —— 因为人会清楚记得"我改过这个文件"，
却记不住"我改了它的**哪几行**"。而这恰好是**校验器最擅长、人最不擅长**的一类检查。
**结论：校验器不是形式主义，它抓的就是"人工自查时最容易骗过自己的那一类错误"。**

**处置（不硬塞，走流程）**：

1. **工具层增强（已登记 M1 前置）**：给 `sync-check.mjs` 加一条检查 ——
   **解析 §7 变更记录里声明的目标版本（`→ X.Y.Z`）并与文件 frontmatter 实际版本比对**。
   本次 FAIL 1-2 就是这种"记录说升到 1.1.1、头部还是 1.1.0"的情形，加这条检查下次能自动抓到。
   > ⚠️ **2026-09-19 更新：这个盲区现在已知有三处，不是一处。** 三处都是"**版本号的载体，而机器不读**"：
   > ① §7 变更记录里声明的目标版本（上述）；② **§6 同步矩阵里写的版本号**（`AUDIT` 行曾连续两轮停在 1.2.0，校验全绿）；
   > ③ **代理定义文件末尾的页脚**（四个代理 frontmatter 已升版、页脚没跟，校验同样全绿 —— 见 `docs/05` §7.3）。
   > **建议合并成一条检查项**：「**同一份文档的所有版本载体必须一致**」，而不是零敲三个补丁。
2. **规则层沉淀（候选，须走流程）**：拟新增一条规则，**编号 `R-B12`（候选）**「同一份受管文件必须整块一致修改」——
   frontmatter 与正文必须同一次完成；`depends_on` 与 `version` 必须同一次完成；**页脚与 frontmatter 必须同一次完成**；收尾一律以 `sync-check` 为准，不以"我记得改过"为准。
   **属 `PROJECT-RULES` 实质变更，须走流程**，不在收尾时顺手改。
   > ✅ **2026-09-19：它拿到了第一个真实案例** —— 四个代理定义的页脚版本号漂移（`docs/05` §7.3）。
   > **这条候选规则原本只是"待走流程"，现在有了实证。**
   > 📌 **编号说明**：本条原记作 `R-B10`，2026-09-19 改号为 `R-B12` —— 因为 `R-B10` 已被**正式签发**给「主进程失败语义」，
   > 而本候选规则**从未签发、也无任何下游引用**，故由候选者让号。**先签发的先占号。**


### 上次 P1 变更的同步明细（1.0.0 → 1.1.0）

| 下游 | 版本变化 | 核心改动 |
|---|---|---|
| `PROJECT-RULES` | 1.0.0 → **1.1.0**（实质） | 新增 **R-C08**（形态差异只能活在令牌层）、**R-C09**（降级形态是一等公民）；改写 **R-E02**（具体化为 `safeStorage`）；新增 **R-E05**（API Key 不得离开主进程，三条硬约束） |
| `CHARTER` | 1.0.0 → **1.1.0** | §二 定位由"泛大众工具"收窄；§三 透明度默认值作废重定 + 新增降级形态与 DPI 已知难点；§六 v0.5 验收放宽；§八 新增密钥存储选型；§九 数据模型补两表；§十二 **R6 关闭**、新增 **R9**；§十三 新增不做项；§十一 里程碑补 M0 |
| `DESIGN-SPEC` | 1.0.0 → **1.1.0** | **修正 G1**（透明度唯一默认值 = 安静 96%）；新增 **§3.7 双形态令牌**（两套形态量对照 + S-01~S-05）；开工前置清单大幅扩充（双套令牌、降级独立对比度检查、DPI 三项）；新增 `SurfaceToggle` 组件 |
| `ARCHITECTURE` | 1.0.0 → **1.1.0** | 新增 **ADR-007 / ADR-008**；新增 **§5.5 双形态渲染架构**；IPC 新增 `capability:report` + `apikey:*` 四通道及**安全约束四条**；目录新增 `surface.css` / `keystore.js`；数据表新增 `window_state`；技术债登记 TD-01~TD-03 |
| `AUDIT` | 1.0.1 → **1.1.0** | **A1、B1、G1 三项闭环**（各附完整落地明细表）；§0 改为"当前进度"表（40 项 / 已闭环 3 / 剩余 37）；§5 处理顺序重排，第二批加优先级 |
| `AGENT-SYSTEM` | 1.0.0 → **1.1.0** | §7 追加 5 行变更；§9 更新版本；`registry.json` 全量核对 |

**代理定义级联**：`design-critic` 1.1.0（+第五组双形态 6 项）｜ `brief-writer` 1.1.0（+API Key 边界）｜ `curator` 1.1.0（+执行清单第 10 步）｜ `collector` 1.0.1（仅依赖跟进）

---

## 8. 风险登记表

| 编号 | 风险 | 概率 | 影响 | 应对 | 状态 |
|---|---|---|---|---|---|
| **R1** | **冷启动**：新用户看到空列表就走 | 高 | 致命 | 预置源包 + 首屏示例简报（先展示后配置）。**⚠️ R6 闭环后，这是当前唯一的头号风险** | 🔴 **头号风险，设计中** |
| **R2** | 抓取层不稳定，某个源挂了 | 高 | 中 | 每源独立健康度；单源失败不阻塞整体；卡片明示失败源数 | 待实现 |
| **R3** | 常驻卡片遮挡/打扰 | 中 | 高 | 半透明 + 可收起 + 不抢焦点 + 全屏自动隐去。**2026-09-22 更新（ADR-012）**：层级本身已结构性解决"**卡片遮挡其他应用窗口**"这一面（置底 ⇒ 永远在最下）；但**反向风险随之而来** —— **卡片会被任何普通窗口盖住，用户可能"忘了它还在"**。缓解：收起态细带 ＋ 位置记忆 ＋ **不做任何自动弹出**（避免用打扰换可见） | 🟡 **设计侧更新**：遮挡他人由 ADR-012 结构性解决；剩余风险是"被遮挡后的可发现性"，归 M1 交互设计 |
| **R4** | Token 成本失控 | 中 | 中 | 五条控制措施（见立项文档 §10）| 待实测 |
| **R5** | API 源授权与合规 | 中 | 中 | 接入前逐个验证；行情类延后 | 待验证 |
| ~~**R6**~~ | ~~用户自配 API Key 的门槛~~ | — | — | ✅ **已闭环（ADR-007）**：纯本地 · 用户自带 Key，不提供中转。配套：一键申请引导 + `safeStorage` 加密 + 泛大众化转入 §3.3 待定区。**残余风险**：非技术朋友仍可能卡在申请 Key 这一步（**已明确接受**，M2 验收标准相应放宽） | ✅ **已闭环** |
| **R7** | 简报质量平庸，"读起来像废话" | 中 | 高 | 压缩率优先（铁律 L1）；宁可少而精 | 持续 |
| **R8** | `backdrop-filter` 技术不可行 | 中 | 高 | M0 最先验证；**预案已升格为一等公民**（ADR-008）——不透明形态仍是完整产品，不是丑版兜底 | ✅ **已关闭（2026-09-18）**：验收 2 **已通过**，且不是靠肉眼 —— 而是**像素级测量**（诊断区高频棋盘格 + `capturePage` + 拉普拉斯方差之比），**并内建了每次自检都跑的校准态**。`CSS.supports('backdrop-filter') = true`、GPU 合成 `enabled` |
| **R9** | **透明窗口 + 多显示器混合缩放的 DPI 缺陷**（`scaleFactor` 读取不可靠、跨屏坐标偏移 30–50px） | 中 | 高 | 并入 M0 验收 6/7/8/9；架构侧 `window_state` 按 `display_id` 分行记忆位置；若不可解 → 触发 ADR-008 降级 | 🟡 **部分降级，但被 2026-09-20 的验收 6 重新拉高**：验收 9 两条独立证据链交叉校验通过，本机 `scaleFactor` **可靠**；"尺寸偏差"的成因已查明为**坐标物理像素对齐**（B12），单屏即复现并已解决。⚠️ **但验收 6 人工实测不通过**（100% 与 125% 两档有问题）→ **缩放适配仍未过关**，读数待补取证（`docs/05` §5.1）。⚠️ **另注：本行"应对"里写的"若不可解 → 触发 ADR-008 降级"，与 §4 失败预案（只列 R8 与验收 7）口径不一致 —— 待验收 6 根因查清后一并对齐**；剩余未验证面仍是双屏混合缩放（验收 7） |
| **R10** | 需求漂移导致范围失控 | 中 | 高 | §0.3 漂移登记机制 + §3.3 待定区 | 已建立 |
| **R12** | **桌面层方案依赖系统内部结构** —— `Progman` / `WorkerW` 非公开 API，系统更新可能打坏（**这正是 ADR-005 当年不做它的理由**） | — | 高 | ① 若采用 ADR-011（桌面层）则必须接受并持续维护；② **2026-09-22 起随 ADR-012 自然规避** —— 卡片重回普通顶层窗口，**不再触碰任何系统内部结构** | ✅ **已规避（2026-09-22）**。⚠️ **本行是补齐登记的**：`MASTER-PLAN` 1.3.0 的变更记录与本表引用都写着"+R12 / §8 R12"，但**该行从未写进本表** —— 同批未落地的还有 `§3.2` 翻案注与 `§5.5` 层级决策行，三处已在本轮一并补齐并留痕（见 `§7`） |
| **R11** | **Electron 平台的"静默失败"类问题**（不报错、不崩溃，只是结果不对：顶层 await 死锁、环境变量导致退化为 Node、尺寸悄悄错 1~4px） | 中 | 高 | ① **唯一启动入口 + 开机心跳**（`tools/launch-electron.mjs`），把"跑了但什么都没发生"变成可判定报警；② 三条硬规则写入 `项目规则.md`（R-B07 / R-B08 / R-B09）与 ADR-009；③ 取证脚本与原始输出留档，结论可复现 | 🟡 **已建立防线**。M0 一口气抓到 5 个（B8–B12），说明这类问题**成片出现**，不是孤例 —— 防线有了，但要靠"每加一层平台能力就补一次探针"持续维护 |

> **编号说明**：原 R9（需求漂移）顺延为 **R10**，把 R9 让给新增的 DPI 风险（v1.1.0）。**两处编号口径以本表为准**（立项文档 §十二 中的"R9"指同一风险，编号一致）。
> v1.2.0 新增 **R11**（平台静默失败）。**它不是某一个具体 bug，而是一整类** —— 所以应对方式也不是"修掉它"，而是"建一道能报警的防线"。
> v1.3.0 登记 **R12**（桌面层方案依赖系统内部结构），**v1.4.0 即被规避**（ADR-012 不再采用桌面层）。
> 这条风险的生命周期很短，但它记录了一次**被接受的代价**：ADR-011 明知它存在仍然选择了桌面层
> （`docs/03` ADR-005 的"理由"栏**从未被推翻**，只是被接受）。⚠️ **登记瑕疵已更正** —— 它当初只被变更记录与其它文档引用，正文从未落笔。

---

## 9. 阶段收尾检查清单

> **每完成一个阶段，逐项打勾。全过才能进下一阶段。**

### 通用（每个阶段都要跑）

- [x] §5 进度看板已更新到当前状态
- [x] §6 同步矩阵的版本号已更新
- [x] §7 变更记录已追加本阶段的所有变更行
- [x] §8 风险登记表已更新（新增/关闭/状态变化）
- [x] 运行 `node tools/sync-check.mjs`，**必须全绿** —— ✅ 2026-09-22 实测达成：**0 错误 0 警告**（9 份文档 ＋ 4 个代理定义）
- [x] ⚠️ **（新增 · 2026-09-22，已完成）修掉 `sync-check` 的 CRLF 静默失效** —— 它会在 CRLF 文件上丢掉 frontmatter 最后一个键，
      使该文件的依赖检查**永久失效且不报错**；`项目规则.md` 因此白躲了整轮。已修（归一化 ＋ CRLF 判 FAIL）＋ **变异测试验证**。详见 §7 本日 ⑪
- [ ] §3.3 待定区里的项已评审（该升的升，该关的关）— M0 不涉及，留待 M1 收尾
- [ ] ⚠️ **（新增 · 2026-09-22）给 `sync-check` 加一道「变更记录 vs 正文」检查** —— 本轮发现 SSOT 自己有三处"变更记录宣告了、正文没落笔"（详见 `§7`「三处留痕纠错」）。**机器现在只查版本号与核对留痕，不查"描述的动作是否真的发生过"**。最小可行做法：变更记录里引用 `§X.Y` 时，校验该节确实存在；对"新增一行/一处"这类动作，要求变更记录同时给出**可搜索的锚点字面量**（本轮已照此写法）。**属工具实质变更，须走流程**

### M0 专项

- [x] **九项**验收标准**逐条留痕**，没有"忘记测了"（见 `docs/05-M0验收记录.md`）
- [x] **九条全部有明确结论**（2026-09-20 起）：**6 通过（1 / 2 / 3 / 4 / 5 / 9）、1 不通过（6）、1 无法验证（7）、1 待重验（8）**
- [x] 双屏混合缩放环境**显式记录为「无法验证」并写明原因**（本机只有 1 块显示器）— **不是跳过**
- [x] `scaleFactor` 的最终读取方式已确定并写入架构文档（`getPrimaryDisplay().scaleFactor`，**须经两条独立证据链交叉校验后才采信**；本机实测可靠）
- [x] **未触发 R8/R9 降级** → 不需要改 `data-surface` 默认值
  （⚠️ **但 2026-09-20 出现验收 6 不通过，可能牵连 R9** —— 见下方新增待办；**"未触发降级"这个结论需在验收 6 定位后复核**）
- [ ] `design-critic` 按新版（1.1.0）第五组跑过一遍双形态检查 — **未执行，遗留项**
- [x] ✅ **人工判定 4 项已测完并回填**（2026-09-20）：验收 4 / 5 → **通过**；验收 6 → **不通过**；验收 8 → **待重验**
- [x] **验收 2 / 3 已由人工判定转为程序自动判定**（2026-09-18，`docs/05` §6.5 第 1、2 步）
- [x] ✅ **验收 3 的裁判校准已完成**（2026-09-19 11:20，`CALIBRATED · 裁判有效`）——
  植入 `show()` 后验收 3 **确实报 `fail`**；此前两次尝试被**环境侧 GPU 崩溃**阻断、被正确判为 `INCONCLUSIVE`。见 `docs/05` §1 验收 3
- [x] ✅ **干净的 `npm run selftest` 已重跑**（2026-09-20 **11:03:42**）—— `_selftest-report.md` 与 `selftest-log.txt` 都已重建，
  摘要 `4/0/0/1/4`、验收 3 恢复 ✅。**它同时是重构后（10:15–10:18）的第一个基准运行 → 重构未引入回归。**
- [ ] 🔴 **（新增 · 当前唯一的收尾阻塞项）补验收 6 的三档读数取证** —— 那两档读数已被同名覆盖，
  现状只有口述。方案与判据见 `docs/05` §5.1。**取证后需同步对齐 §4 失败预案与 §8 R9 的降级口径。**
- [ ] ⚠️ **（新增）实跑验证拖动把手 `.grip`** —— 含三项：透明 + 不激活窗口能否拖 / 拖动是否抢焦点 / 松手是否停稳。
  **2026-09-22 注**：ADR-012 撤销了"桌面层使 CSS drag 必然失效"这条推论 ⇒ **该验证重新有意义**；但 **B-07（疑为 `resizable: false`）仍是独立缺陷**，见 `§5.4`
- [ ] 接入第二块显示器后补测验收 7
- [x] M0 期间新发现的 5 项（B8–B12）已登记并处置（见 `docs/04-项目审查报告.md` §6）
- [x] ✅ **（2026-09-22 结案）M0 追加验收 A10「桌面层挂载可行性」** —— **判据① FAIL ⇒ 桌面层不可行**（机器判定；分辨力经校准；有内建正对照；四种配置全灭）。**该结论触发了 ADR-011 自己预登记的重审条件①**；经用户裁定改走 **ADR-012「置底顶层窗口」**，**实测可行**（可见 ＋ 可点 ＋ 紧贴桌面之上）。详见 `§4` M0 节末「A10 结论」与 `§8` **R12**
- [x] ✅ **（新增 · 2026-09-20）需求 5「移除展开全部」已实现** —— 代码 2 处（`card.html` / `card.js`）＋ 文档级联 6 处（`docs/01` §四、`docs/02` §4.2 与 §4.3，两份文档各自升版并补留痕）
- [x] ✅ **（2026-09-22 完成）验收 1 措辞与验收 4 命题已按 ADR-012 改写** —— A10 结论已出（桌面层不可行、置底方案实测可行），**改写前提满足**。**验收 1** 定语由"置顶"改为"**置底**"；**验收 4 命题恢复有效**（卡片重回普通顶层窗口）；**验收 2 / 6 结论重新可外推**。详见 `§4` M0 节末对照表与 `docs/05` §1
- [ ] 🔴 **（新增 · 2026-09-20）跑一次拖动诊断（B-07 取证）** —— 两次运行 + 四件事记录，
  **无论结果如何都要回写 `docs/05` §1 验收 8**。操作单见 `m0-probe/README.md`「拖动把手的诊断」
- [x] **（新增 · 2026-09-20）`docs/02` 留痕纠错已完成** —— 该文件 1.2.0 声称改了 2 处、实际只落 1 处（§4.3 从未落盘）。**这是"变更记录宣告了没发生的事"，比没改更糟**，已修正并记入 1.3.0

> **M0 可以收尾的判定依据**：通用清单 5/6 项完成，专项清单里**机器能做的全部做完**。
> 未打勾的项里，**"需要人或需要硬件"的那些**不构成"M0 未完成"——按 §4 的精神，
> "没条件测"和"没测"都如实留痕，比假装通过有价值得多。
>
> ⚠️ **2026-09-20 更新：这个判定依据现在不够用了。** 新增的 **A10（桌面层挂载可行性）不属于"需要人或需要硬件"** ——
> 它是一项**全新的、必须实测的地基不确定性**，并且**它会决定验收 1 与验收 4 的最终口径**。
> 于是 M0 的收尾条件从"补完验收 6 取证"变成了**两件**：**验收 6 三档读数取证 ＋ A10 有结论**。
>
> ⚠️ **2026-09-22 更新：A10 已有结论**（桌面层不可行 ⇒ ADR-012 置底方案实测可行），**这一件从清单上划掉**。
> 收尾条件因此变成 **"两件但性质不同"**：① **验收 6 三档读数取证**（需要人跑）；② **B-07 拖动缺陷修复**（工程活）。
> **ADR-012 撤销了 ADR-011 带来的四项连带影响 ⇒ M0 的收尾面比 2026-09-20 时更窄，不是更宽。**

### M1 专项

- [ ] 连续三天实际使用记录 —— **2026-09-25 注**：v0.1.2 已发布并日常在用，但**没有连续三天的书面记录**；机器判不了，只能由用户自证
- [ ] Token 实测消耗数据已记录到立项文档 §10 —— **2026-09-25 注**：AI 摘要已落地，用量**记账链路已通**（每份简报落 `token_used`，⚙ 面板直接显示当天用量；`tools/test-ai-brief.mjs` 有用量断言）。**待真机跑一天后回填 `docs/01` §10 的真实数字**
- [ ] 铁律 L1（压缩率）实测达标（≤ 5%）—— **2026-09-25 注**：**现在可测了** —— 简报的 `raw_count` / `kept_count` 都落库，面板里直接显示「候选 N 条 → 精选 M 条」。**同样待真机跑一天之后回填真实数字**

---

## 10. 项目地图

```
项目计划工程书.md          ← 你在这里 · 唯一权威源
├── 项目规则.md            ← 做事的方法与底线（34 → 37 条）
├── README.md             ← 项目入口索引
├── docs/
│   ├── 01-立项文档.md              ← 做什么、为什么
│   ├── 02-设计研究与页面架构规范.md  ← 长什么样、怎么动
│   ├── 03-工程架构文档.md           ← 怎么搭、怎么扩展
│   ├── 04-项目审查报告.md           ← 还缺什么（持续更新的问题登记表）
│   └── 05-M0验收记录.md             ← M0 结论（九条验收留痕）
├── m0-probe/              ← M0 探针工程（不是产品，验证用）
│   ├── README.md                   ← 工程说明 + 工具链坑位
│   ├── tools/
│   │   ├── launch-electron.mjs     ← ★ 唯一 Electron 启动入口（净化环境 + 心跳守护）
│   │   └── win-display-facts.ps1   ← ★ Windows 独立裁判（DPI 交叉校验）
│   ├── src/                        ← 探针实现（main / preload / renderer）
│   ├── experiments/                ← 取证实验脚本 + 说明（可复现）
│   └── report/                     ← 原始证据（自检报告、实验输出、心跳）
├── agents/                ← 会自己学习的干活单元
│   ├── README.md                   ← 机制说明
│   ├── _template.md                ← 代理定义模板
│   ├── registry.json               ← 注册表
│   ├── collector.md                ← 采集代理
│   ├── brief-writer.md             ← 简报生成代理
│   ├── design-critic.md            ← 设计审查代理
│   └── curator.md                  ← 代理维护者
└── tools/
    ├── sync-check.mjs      ← 同步校验工具
    └── sync-report.txt     ← 最近一次校验结果
```

M1 产品工程（v0.1.x，2026-09-25 补记 —— 原来这份地图里**没有它**）
```
├── src/
│   ├── main/               ← 主进程（窗口 / 托盘 / IPC / 调度 / 自更新）
│   ├── preload/            ← 隔离层（内联通道表 + 开机自检）
│   ├── renderer/           ← 卡片界面（卡片 / 视图模型 / 样式 / 图标）
│   ├── ingest/             ← 抓取与解析（零依赖手写解析器 ＋ 预置源清单 ＋ 适配器）
│   ├── store/              ← 数据库（node:sqlite；schema / 迁移 / 查询）
│   └── shared/             ← 纯函数层（配额 / 更新 / 运行状态 / 日志；★ 不许 import electron）
└── tools/
    ├── launch.mjs          ← ★ 唯一 Electron 启动入口（继承 M0 的 R-B09）
    ├── service.mjs         ← 后台启动 / 停止 / 状态 / 日志
    ├── run-ingest.mjs      ← 脱离 GUI 跑抓取（抓取层可维护的前提）
    ├── test-all.mjs        ← 离线考裁判：198 条断言
    ├── test-mutants.mjs    ← 变异测试：44 个变异体（证明断言真的抓得住 bug）
    ├── dist.mjs            ← 打包
    ├── check-dist.mjs      ← ★ 打包隐私门（产物必须严格等于 src/ + package.json）
    └── release.mjs         ← 生成发布清单（带 sha256 ＋ 版本号守卫）
```

> ⚠️ **这份地图原来只画到 M0 探针工程** —— 产品工程（`src/` 与它的工具链）**一个字都没有**，
> 而它已经发了三个版本。**「地图上没有的地方」最容易变成没人维护的地方**，故在此补记。

> **★ 标记的两个文件是"证据链"**：`launch-electron.mjs` 保证"我们测的确实是 Electron"，
> `win-display-facts.ps1` 保证"Electron 报的缩放比有外部基准可对"。
> **M1 建产品工程时必须把它们搬过去** —— 少了任何一个，M0 的结论就重新变成"只凭它自己说"。


**待创建（尚未建立）**：`docs/00-术语表.md`（审查报告 E1）、`package.json` 的 `check` 脚本（审查报告 F3）、`arch-critic` 代理（M1 阶段）。

---

*文档版本：1.6.0 ｜ 状态：ACTIVE ｜ 更新时间：2026-09-25*
*本次为实质变更（P2）：**「AI 摘要」补齐 ⇒ M1 交付物四项全齐**（M1 仍为「部分交付」：验收标准无记录、前置依赖未满足）。详见 §4 / §5.2c / §7。*
