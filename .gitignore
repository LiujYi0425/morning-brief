# =====================================================================
# .gitignore —— 晨报机
# =====================================================================

# ── 依赖 ────────────────────────────────────────────────────────────
# 约 250MB，其中绝大部分是 Electron 的二进制。
# 别人 clone 之后跑一次 `npm install` 就能拿到，不需要进仓库。
node_modules/
.npm-cache/

# ── ★★ 用户的简报数据 —— 绝不能进仓库 ──────────────────────────────
# 数据目录现在就在项目里（`data/`，见 src/shared/runtime-state.js
# 的 dataDirOf）。这是"搬走/备份只搬一个目录"的代价：
# **一个疏忽就会把私人简报提交到公开仓库。**
#   · 提交前务必确认 `git status` 里没有 data/ 下的东西
#   · 打包给别人时也必须排除它（见 package.json 的 build.files 排除项）
data/
# 旧版遗留（早期把 MB_DATA_DIR 指到过这里）
.data/

# ── 运行与测试产物 ──────────────────────────────────────────────────
# report/ 里是考裁判的输出：**结论进仓库、运行日志不进**（同父仓库的口径）。
#   运行日志里含本机绝对路径与用户名，公开出去既泄露隐私、对别人也毫无用处。
report/
*.log

# ── 编辑器 / 系统 ──────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp

# ── 临时文件 ────────────────────────────────────────────────────────
*.tmp
.tmp-*
*.bak

# ── 密钥 / 凭据兜底 ─────────────────────────────────────────────────
.env
.env.*
*.pem
*.key
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub

# ── 打包产物（以后接 electron-builder 时用得上）──────────────────────
dist/
out/
release/
