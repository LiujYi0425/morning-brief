---
agent_id: REPLACE-ME
version: 1.0.0
type: worker
updated: 2026-09-18
status: DRAFT
trigger: REPLACE-ME（什么情况下该调用它）
depends_on: ARCHITECTURE@1.0.0
eval_score: null
run_count: 0
success_count: 0
---

# 代理 · REPLACE-ME

> 复制本文件为 `<agent_id>.md`，替换所有 `REPLACE-ME`，然后在 `registry.json` 注册，
> 并在 `agents/README.md` §9 的代理清单里加一行。

---

## 职责

**一句话**：REPLACE-ME

**具体做的事**：

1. REPLACE-ME
2. REPLACE-ME

---

## 不负责

> 这一节和「职责」同等重要。**边界不清的代理一定会慢慢蔓延。**

- ❌ REPLACE-ME（明说它不管什么）
- ❌ 不修改用户的原始数据（规则 R-E01）
- ❌ 不擅自扩大职责范围（规则 R-F04）

---

## 输入契约

调用本代理必须提供：

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| REPLACE-ME | REPLACE-ME | 是 | REPLACE-ME |

**前置条件**：REPLACE-ME

---

## 输出契约

本代理保证产出：

```
REPLACE-ME（输出结构）
```

**质量要求**：

- REPLACE-ME

**失败时的行为**：REPLACE-ME（必须有失败路径，规则 R-B04）

---

## 执行清单

> **这是本代理的操作规则。Lessons 里转化出来的新规则会合入这里。**
> 每次运行都按此清单执行，不要凭记忆。

1. REPLACE-ME
2. REPLACE-ME
3. REPLACE-ME

---

## 自检清单

交付前必须逐项确认：

- [ ] REPLACE-ME
- [ ] 输出符合输出契约的结构
- [ ] 自评 `confidence` 已给出，不确定的地方已明说（规则 R-A04）
- [ ] 本次的新教训已记录到下方 Lessons（没有就写"无"，不允许留空）

---

## Lessons

> **每次任务后追加一行。**「转化成的规则」一列不允许填"下次注意"——
> 必须是下次能自动执行的具体动作，并且要真的写进上面的「执行清单」。

| 日期 | 场景 | 踩到的坑 / 发现 | 转化成的规则 | 应用版本 |
|---|---|---|---|---|
| — | — | — | — | — |

---

*版本：1.0.0 ｜ 类型：worker ｜ 更新时间：2026-09-18*
