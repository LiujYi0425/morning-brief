主进程失败语义 · 考裁判
生成时间：2026-09-20T04:00:06.227Z
结果：PASS

┌─ 主进程失败语义 · 考裁判（落盘 + 退出策略）─────────────────
│ 被测：src/main/crashlog.js
│ 策略：uncaughtException → 同步落盘 → exit(1)
│       unhandledRejection → 同步落盘 → 不退出但可见
│ ⚠️ 范围：真的触发崩溃、真的读落盘内容与退出码（纯 Node，不需要 Electron）
└────────────────────────────────────────────────────────────

--- 第一关 · uncaughtException → 落盘 + exit(1) ---
  ✓ 退出码 = 1
  ✓ 崩溃标记文件已生成
  ✓ kind = uncaughtException
  ✓ 记录了原始错误消息
  ✓ fatal = true（致命由种类决定，不由调用方随口指定）
  ✓ 保留了调用栈
  ✓ 带了时间 / pid / node 版本
  ✓ crash-log.jsonl 追加了 1 条

--- 第二关 · 崩溃标记必须"报告一次就消失" ---
  ✓ 第一次取到了那条崩溃记录
  ✓ 第二次取返回 null —— 不会重复报告同一场崩溃
  ✓ 取走后标记文件已被删除

--- 第三关 · unhandledRejection → 落盘 + 不退出 ---
  ✓ 退出码 = 0（不退出，应用继续活着）
  ✓ **没有**生成致命崩溃标记（它不该被当成崩溃）
  ✓ rejection-log.txt 记录了 1 条
  ✓ kind = unhandledRejection
  ✓ fatal = false（非致命）
  ✓ 记录了原始拒绝原因

--- 第四关 · 纯函数边界（不落盘、不退出）---
  ✓ 非 Error 的抛出物被归一成 Error
  ✓ fatal 由 kind 推导
  ✓ kind=unhandledRejection 时 fatal=false
  ✓ describeCrash(null) 安全返回 none
  ✓ describeCrash 输出可读的一行

--- 第五关 · 考裁判（变异体必须落网）---
  ✓ 变异体「落盘但不退出」的退出码 = 0
  ✓ 变异体一样落了盘（证明第一关能区分的是"退不退出"，不是"写没写文件"）
      ⇒ 结论：第一关"退出码必须 = 1"这条断言**有判别力** ——
        一个不退出实现会被它当场抓住，而不是安静地报绿。

┌─ 结论 ──────────────────────────────────────────────────────
│ ✓ PASS —— 落盘与退出策略按设计工作，且断言已被证明有判别力
│ ⚠️ 范围：本脚本验的是 crashlog.js 的策略本体。
│    "index.js 有没有正确装上它"仍需一次真启动（沙箱可能不允许）。
└────────────────────────────────────────────────────────────
