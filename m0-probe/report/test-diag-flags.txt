┌─ 诊断开关解析契约 · 离线考裁判 ─────────────────────────────
│ 被测：src/main/diag-flags.js
│ 环境变量：M0_DIAG_RESIZABLE
│ ⚠️ 范围：只考"给定输入解析成什么" + "源码接线是否还在"。
│          不考"终端到底有没有把变量传进去"（那件事只能看运行时留痕）。
└────────────────────────────────────────────────────────────

【一】被测实现 parseFlag —— 24 条用例
  （全部通过，未逐条列出）

【二】接线层 diagResizable —— 从环境对象取值
  ✓ 空对象（变量未设）读到 false  →  false
  ✓ 设成 '1' 读到 true  →  true
  ✓ 设成 '0' 读到 false  →  false
  ✓ env 传 null 不抛错且为 false  →  false
  ✓ env 传 undefined 不抛错且为 false  →  false
  ✓ 不传参（走 process.env）返回布尔  →  "boolean"

【三】源码接线检查 —— src/main/window.js
  ✓ 卡片窗口构造函数里不再有写死的 resizable: false  →  false
  ✓ 构造函数里引用的是本地常量 resizable  →  true
  ✓ 本地常量由 diagResizable() 计算  →  true
  ✓ 已 import 该模块  →  true
  ✓ 无条件诊断留痕仍在（"开关没生效"必须留得下痕迹）  →  true

【四】⭐ 变异测试 —— 用例表有判别力吗（存活即判 FAIL）
  ✓ M1 不看输入，一律返回 true → 落网（15 条用例抓住）
  ✓ M2 不看输入，一律返回 false → 落网（9 条用例抓住）
  ✓ M3 做了归一但漏掉大小写（不 toLowerCase） → 落网（4 条用例抓住）

────────────────────────────────────────────────────────────
结论：✅ PASS —— 24 条用例全过，3 个变异体全部落网，接线完好。
────────────────────────────────────────────────────────────
