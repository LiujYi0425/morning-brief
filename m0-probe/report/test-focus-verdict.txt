验收 3 · 焦点判定逻辑 · 离线考裁判
生成时间：2026-09-20T04:00:04.447Z
结果：PASS

┌─ 验收 3 · 焦点判定逻辑 · 离线考裁判 ────────────────────────
│ 被测：src/main/focus-verdict.js · computeFocusVerdict()
│ 用例：10 条　变异体：3 个
│ ⚠️ 范围：只考"判定"，不考"采集" —— 真实 focus 事件仍需 npm run calibrate
└────────────────────────────────────────────────────────────

--- 第一关 · 判定正确性 ---
  ✓ 测试还没结束 → running  →  running
  ✓ 正常态：显示过 1 次、零抢焦点 → pass  →  pass
  ✓ ★ 植入已知错误：show() 抢到焦点 → fail  →  fail
  ✓ 面板被挤得失焦（卡片没收到 focus 事件）→ fail  →  fail
  ✓ 两种抢焦点信号同时出现 → fail  →  fail
  ✓ 测试期内没显示过卡片 → invalid（不是 pass）  →  invalid
  ✓ ★ 判定优先级：有 focus 事件但 cardShowCount=0 → fail（不是 invalid）  →  fail
  ✓ ★ 判定优先级（第二条）：只有面板失焦、且 cardShowCount=0 → fail  →  fail
  ✓ 字段缺失（undefined）不崩、按 0 处理 → invalid  →  invalid
  ✓ 入参整个是 null → 安全降级为 running  →  running

--- 第二关 · 用例表本身的判别力（变异测试）---
  ✓ M1 已被抓住（9 条用例报警）—— 恒返回 pass（"永远绿灯"的极致形态）
  ✓ M2 已被抓住（2 条用例报警）—— 只判两态 —— 把 invalid 当成 pass
  ✓ M3 已被抓住（2 条用例报警）—— 判定顺序反了 —— 先看"有没有显示"再看"抢没抢"

┌─ 结论 ──────────────────────────────────────────────────────
│ ✓ PASS —— 判定正确，且用例表已被证明有判别力（3/3 变异体全部落网）
│ ⚠️ 但这只覆盖"判定"。真实 focus 事件会不会发生，仍需 npm run calibrate。
└────────────────────────────────────────────────────────────
