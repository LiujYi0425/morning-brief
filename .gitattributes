# 统一换行符为 LF —— 仓库内与工作区都是 LF，避免 Windows 上反复出现
# "LF will be replaced by CRLF" 的噪音，也避免协作者之间产生整文件级的假 diff。
* text=auto eol=lf

# 二进制文件不做换行符转换
*.png binary
*.jpg binary
*.ico binary
*.exe binary
*.dll binary
